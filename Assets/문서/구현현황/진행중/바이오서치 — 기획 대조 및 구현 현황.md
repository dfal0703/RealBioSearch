# 바이오서치 — 기획 대조 및 구현 현황

작성일: 2026-07-16
목적: `Assets/문서/기획문서/`의 3개 문서(핵심 기획 / 전체 기획 정리 / 개발 구현 지시서)를 항목
하나하나 다시 읽고, 실제 코드(`Assets/Script`, `Assets/Prefabs`, `Assets/Data`)와 대조해
"기획서에 있는데 아직 없는 것" / "기획서와 다르게 구현된 것" / "기획서에 없는데 구현 중
새로 생긴 것"을 명확히 구분해서 기록한다.

이전 진행상황 문서(Stage 1~8 + 이후 UX 보완까지의 상세 작업 로그, 22개 절)는
`구현현황/지난거/07.16 현황/바이오서치 — 구현 계획.md`로 옮겨뒀다 - "왜 이렇게
구현했는지"의 세부 경위가 필요하면 그 문서를 참고. 이 문서는 그 로그를 반복하지 않고,
기획서 원문 기준으로 다시 검증한 결과만 담는다. 앞으로의 신규 작업 로그는 이 문서에
이어서 기록한다.

---

## 1. 핵심 게임 루프 (핵심 기획.md / 전체 기획 정리.md 1장)

> CLI 요청 수신 → 검사체 입장 → 문서 확인·대화 → 의심 부위 추론 → 검사 부위·방식 선택 →
> 검사 실행·결과 생성 → 자료 대조 후 추가 검사/종료 → 감염 여부·부위 보고

**구현 상태: 전체 루프 존재.** 8단계 전부 코드로 이어져 있고, 전부 사례 하나(CASE-001,
김도윤)로 실제 플레이 가능:
- CLI 요청 수신 - `CaseSessionService.Initialize()` (`Script/Service/CaseSessionService.cs`)
- 검사체 입장 - 3D 룸(`ssh.unity`) + `ComputerViewController`(WASD 카메라 전환)
- 문서 확인 - `report` 명령(`DialogueService.ExecuteReport()`) + 라이브러리 열람
- 대화 - `ask <키워드>` 명령(`DialogueService.ExecuteAsk()`)
- 검사 부위·방식 선택/실행 - `ExamControlPanel` + `ExamService.RunExam()`
- 자료 대조 - `LibraryPanel`(파일 탐색기 UI)
- 최종 보고 - `FinalReportPanel` + `CaseSessionService.CompleteCase()`

**차이점(의도적 확장)**: "검사체 입장"이 기획서 문구상으론 추상적 3D 연출인데, 실제로는
WASD 4포인트 카메라 전환 + 렌더텍스처 모니터 화면이라는 구체적인 시스템으로 구현됨(구현
계획.md 0장 - 최초엔 "2D 연출로 대체" 판단이었다가 실제 플레이 중 3D 룸으로 방향 전환).
기획서를 위반하는 게 아니라 더 구체적으로 실현한 것.

---

## 2. 검사체 설정 (전체 기획 정리.md 2~3장)

기획서: 검사체는 정상 민간인/질환자/기생체 감염자/일부·완전 지배당한 사람/공생체(향후) 중
하나일 수 있고, 기생체는 등급에 따라 숙주 모방 정밀도가 다르며 자극 시 변이한다.

**구현 상태: 대폭 축소(의도적).** 현재 사례는 "감염/비감염" 이진 판정만 존재
(`CaseFileDefinitionData.isInfected`) - 지배 정도, 기생체 등급, 공생체 개념은 전혀 없음.
개발 구현 지시서 11장이 "공생체 판정"을 명시적으로 초기 범위 제외 항목으로 못 박아뒀으므로
기획 위반이 아니라 **정확히 기획서가 지시한 축소**임.

---

## 3. 검사체 접수 · 진단 보고서 (전체 기획 정리.md 4장)

기획서 진단 보고서 10개 필드: 이름/성별/나이/신분/직업/검사 요청 경위/기존 질환/신고된
증상/최근 행동 이상/**제출된 무고 증명 자료**.

**구현 상태: 9/10 필드 구현, 1개 누락.** `CaseFileData`(`Script/Data/CaseFile.cs`)에
앞의 9개 필드는 전부 있음(`Assets/Data/CaseFileTemplate.json`에 실제 값 채워짐). **"제출된
무고 증명 자료"는 어디에도 구현돼 있지 않다** - Stage 2 계획 당시엔 "파일이라 첨부파일
목록 쪽으로 넣는다"고 판단했었는데, Stage 3에서 첨부파일 목록 자체가 `library` 하나로
통합된 뒤 이 항목을 실제로 채워 넣는 작업이 빠진 채로 지금까지 넘어옴. **실제 미구현 -
튜토리얼 사례에 "무고 증명 자료" 문서 한 건을 라이브러리에 추가하면 해결됨.**

---

## 4. 대화 시스템 (전체 기획 정리.md 5장)

기획서: 대화로 통증 부위/이질감/감각 이상/기억 공백/행동 변화/특정 자극 거부감/몸 안
움직임 느낌/보고서와 다른 증언을 확인. 모든 대화는 자동으로 "대화 로그 파일"에 기록.
녹음 활성화 시 음성 파일/파형/주파수 분석/복수 음성 자료 추가 생성.

**구현 상태**:
- 대화 자동 로그 기록 - **구현됨**. `DialogueService.ExecuteAsk()` → `CaseSessionService.
  AppendToLog()`가 "대화 로그" 파일 하나에 계속 이어붙임(기획서 "모든 대화는 자동으로
  대화 로그 파일에 기록됩니다" 문구를 "파일 하나"로 정확히 해석해 구현).
- 대화 내용 자체 - 키워드 8개(기침/가슴/호흡/발한/건강/직업/동료/격리)로 축소 구현
  (`Assets/Data/CaseFileDefinition.json`). 기획서가 나열한 8개 대화 주제와 실제 키워드가
  1:1 대응하진 않지만 같은 범주(증상/이질감/행동 변화/증언)를 담고 있음.
- **녹음 시스템 - 미구현.** 음성 파일/파형/주파수 분석/복수 음성 감지 전부 없음. 시스템
  자체가 없어서(시스템과 콘텐츠 분리 원칙상 의도적 보류) CLI에 "녹음 활성화" 명령 자체도
  없음(`DialogueService.Commands = {"help","report","ask"}`뿐).

---

## 5. 검사 시스템 (전체 기획 정리.md 6장)

기획서 검사 방식 8종: 문진/관찰/음향/전기/압력·진동/촬영·투과/채취/극단적 자극.
기획서 검사 대상 장기 11종: 뇌/심장/폐/간/위/대장/신장/안구/피부/근육/신경계.

**구현 상태: 방식 3종 + 부위 5종으로 축소(의도적)**.
- `ExamService.Methods = {"관찰 검사", "음향 검사", "촬영·투과 검사"}` - 8종 중 3종만.
  전기/압력·진동/채취/극단적 자극 미구현.
- `ExamService.Organs = {"뇌","심장","폐","위","피부"}` - 11종 중 5종만. 간/대장/신장/
  안구/근육/신경계 미구현.
- "문진 검사"는 별도 구현 없지만, CLI의 `ask` 명령(대화 시스템)이 사실상 그 역할을 이미
  담당하고 있음 - `ExamControlPanel` 드롭다운 목록에는 없지만 기능적으로는 존재.

개발 구현 지시서 6장 "초기 구현에서는 소수의 장기와 검사 방식만 사용"이라는 명시적 지시와
정확히 일치 - 축소 자체는 기획 위반이 아님. 다만 **"압력·진동"/"전기"/"채취"/"극단적 자극"
검사가 하나도 없어서, "발견 가능성은 높지만 상해·사망·변이 위험이 큰" 고위험 검사군(극단적
자극)이 지금 있는 3종(관찰/음향/촬영)의 "강" 강도로만 대리 표현되고 있다는 점은 참고할
부분.**

---

## 6. 검사 비용과 위험 (전체 기획 정리.md 7장)

기획서: 검사 시간 소요(현실:게임 시간 비율), 위험한 검사 시 숨겨진 체력 감소(5단계 간접
표시: 안정/경미한 이상/불안정/위험/위급), 기생체 자극도는 말투 변화/카메라 노이즈/비정상
신체 움직임/장기 반응 증가/갑작스러운 침묵/공격 전조로 암시.

**구현 상태**:
- 시간 - **구현됨**. `CaseTimeService`(현실 1초=게임 1분, 총 240분, 패시브+검사별 능동
  소모) - 기획서의 "행동 포인트 대신 현실:게임 시간 비율" 지시를 그대로 따름.
- 건강 상태 5단계 - **구현됨**. `HealthService.HealthState` enum이 기획서 예시 문구
  (안정/경미한 이상/불안정/위험/위급)를 그대로 사용, `HealthStatusPanel`에 표시.
- **기생체 자극도 암시 연출 - 부분 구현.** `MutationService`가 숫자 누적→임계치 즉발
  트리거로 존재하긴 하지만, 기획서가 예로 든 6가지 암시 신호(말투 변화/카메라 노이즈/
  비정상 신체 움직임/장기 반응 증가/갑작스러운 침묵/공격 전조) 중 **어느 것도 점진적으로
  구현돼 있지 않다** - 지금은 임계치를 넘는 "순간" `SubjectMonitorPanel` 화면이 붉게
  바뀌는 것 하나뿐, 그 전 단계의 "전조" 연출이 없음. 개발 구현 지시서 8장 완료 기준
  ("위험의 전조를 인식하고")과 가장 직접적으로 어긋나는 부분 - **가장 우선순위 높은
  실제 gap.**

---

## 7. 변이와 비상 상황 (전체 기획 정리.md 8장)

기획서 대응 방식 4종: 3D 공간의 비상 버튼 작동 / 검사 장비 긴급 정지 / 검사실 봉쇄 /
제압 절차 실행.

**구현 상태: 4종 → 1개 행동으로 통합(의도적 단순화)**. `MutationService.
ResolveEmergency()` 버튼 하나(3D 룸의 "계기판" 오브젝트, `EmergencyPanelController`)를
누르면 위 4가지가 전부 한 번에 처리된 것으로 취급 - 개발 구현 지시서 자체가 "변이는 일반적인
전투 시스템이 아니다"라고 강조하고 별도 전투/절차 시스템을 요구하지 않으므로 범위 안의
단순화로 판단. 사고 기록(`CaseFileEntryType.Incident`)도 생성됨 - "관련 이미지와 사고 기록
생성" 중 이미지는 미구현(사진 시스템 자체 없음), 텍스트 기록은 구현됨.

---

## 8. 검사 결과와 파일 시스템 (전체 기획 정리.md 9장)

기획서 4대 분류: 문서 파일 / 대화·오디오 파일 / 이미지 파일 / 수치·그래프 파일.

**구현 상태**: `CaseFileEntryType` enum에 `Document/Dialogue/Audio/Image/Numeric` 5개가
그대로 존재하지만, **실제로 콘텐츠가 채워지는 건 `Document`/`Dialogue`뿐이다.**
`Audio`/`Image`/`Numeric`은 타입만 있고 그 자료를 만드는 시스템(녹음/촬영/기계 계측)이
없어서 항상 빈 폴더 - `LibraryPanel`은 항목이 하나도 없는 타입 폴더 자체를 안 보여주므로
지금은 이 3개 폴더가 아예 안 보임(정상 동작, 버그 아님).

추가로 기획서 4대 분류에 없는 폴더 2개가 구현 과정에서 새로 생김:
- `ExamResult`(검사 결과) - 원래 "문서" 분류에 속한다고 봤다가, 사용자가 "문서에 넣지
  말고 검사 결과 폴더를 따로 만들어달라"고 명시적으로 요청해서 분리.
- `Incident`(사고 기록) - 6단계 비상 대응 완료 시 생성.

둘 다 기획서를 위반한다기보다 "검사 결과 보고서"/"사고 보고서"라는 기획서의 상위 개념을
UI 편의상 더 세분화한 것으로 볼 수 있음.

---

## 9. 최종 판정 (전체 기획 정리.md 10장)

기획서: 감염 여부 + 감염 부위만 초기 범위. 확장 항목(기생체 분류/위험 등급/생존 상태/
제거 승인/격리 필요)은 이후로 미룸.

**구현 상태: 정확히 일치.** `FinalReportPanel`이 감염 여부(2택)/감염 부위(5택) 드롭다운 +
제출 버튼만 제공. 정답 대조(`CaseFileDefinitionData.isInfected`/`infectedRegion`) +
검사체 상태(`HealthService.CurrentState`)를 함께 반영한 결과 문구 표시까지 완료 기준
("정확성과 검사체 상태에 따른 결과") 그대로 충족.

---

## 10. 화면 구성 (핵심 기획.md / 전체 기획 정리.md 11장)

기획서 6영역: 좌상단 3D 검사실 카메라 / 좌중 시각 메모 / 좌하 건강 상태+최종 보고서 /
중앙 검사 제어 / 우상단 라이브러리 / 우하단 CLI.

**구현 상태: 레이아웃 100% 일치.** `ComputerScreenCanvas.prefab`의 7개 슬롯
(`SubjectMonitorSlot`/`VisualMemoSlot`/`HealthStatusSlot`/`FinalReportSlot`/
`ExamControlSlot`/`LibrarySlot`/`CLISlot`)이 기획서 6영역(건강+최종보고서가 한 영역을
상하로 나눔)과 정확히 대응.

각 영역별 기능 완성도:
- **좌상단(SubjectMonitorPanel)**: "검사체 입장/외형 관찰/검사 반응/변이 연출/공격 상황/
  검사실 상태 확인" 중 변이 연출(색 반전)만 있고, 나머지(외형 관찰/검사 반응/공격 상황)는
  **미구현** - 애초에 3D 검사체 모델 자체가 없어서(구현 계획.md에서 여러 차례 확인된 사실)
  구조적으로 못 만드는 상태.
- **좌중(VisualMemoPanel)**: 완전 미구현, Stage 1 플레이스홀더 그대로(`Script/UI/Panels/
  VisualMemoPanel.cs` 확인 - 배경만 있고 로직 없음). 이미지 자료 자체가 없어서(위 6장/8장
  참고) 배치할 대상이 아직 없다는 게 이유지만, **패널 자체가 손도 안 대진 상태라는 점은
  명확히 gap.**
- **좌하(HealthStatusPanel/FinalReportPanel)**: 완전 구현.
- **중앙(ExamControlPanel)**: 완전 구현(로딩 연출 포함).
- **우상단(LibraryPanel)**: 완전 구현(파일 탐색기 스타일 + 튜토리얼 문서 강조).
- **우하단(CLIPanel)**: 자유 텍스트 입력 기반으로 완전 구현.

---

## 11. 우측 하단 CLI 세부 기능 (전체 기획 정리.md 11장)

기획서 7개 기능: 검사 요청 수신/시스템 메시지/명령어 입력/검사체와의 대화/녹음 활성화/
파일 및 장비 제어/기관 또는 NPC와의 연락.

**구현 상태: 4/7 구현.** 앞 4개(요청 수신/시스템 메시지/명령어 입력/대화)는 구현됨. 뒤 3개
(녹음 활성화/파일·장비 제어/기관·NPC 연락)는 **미구현** - 전부 그 대상이 되는 시스템(녹음/
장비/조직 서사)이 아직 없어서 자연히 없음(시스템과 콘텐츠 분리 원칙에 따라 의도적 보류로
분류되지만, 기획서에 명시된 항목이 빠져있다는 사실 자체는 기록해둘 필요가 있음).

---

## 12. 기생체 도감 (전체 기획 정리.md 12장)

**구현 상태: 완전 미구현.** 도감/데이터베이스 개념 자체가 코드 어디에도 없음. 개발 구현
지시서 11장 제외 요소에 "기생체 도감의 전체 기능"이 명시돼 있어 초기 범위 밖이 맞음 - 다만
"전체 기능"이 아닌 최소 버전(예: 발견된 장기 하나만 기록)조차 없다는 점은 참고.

---

## 13. 서사와 전개 (전체 기획 정리.md 13장)

**구현 상태: 완전 미구현(의도적).** CLI 나레이션 외 나머지(네임드 NPC/검사체 재등장/숨겨진
파일/삭제된 기록/조직 문서/공생 사례)는 개발 구현 지시서 11장에 전부 제외 대상으로 명시돼
있어 정확히 일치.

---

## 14. 개발 원칙 준수 여부 (개발 구현 지시서 2장)

- **핵심 루프 우선** - 준수. 부가 기능(도감/서사/녹음)보다 루프 완성에 집중.
- **단계별 플레이 가능 상태 유지** - 준수. Stage 1~8이 각각 독립적으로 플레이 검증됨
  (구현 계획.md 로그에 매 스테이지 "완료 기준" 충족 확인 기록).
- **시스템과 콘텐츠 분리** - 대체로 준수(위험도 테이블 등은 시스템/서비스에, 사례별 내용은
  JSON 정의 데이터에). 단, 앞서 언급한 "제출된 무고 증명 자료" 누락은 이 분리를 지키다가
  실제 콘텐츠 등록을 빠뜨린 사례.
- **2D/3D 역할 분리** - 준수. 3D 룸은 WASD 이동 + 계기판 클릭 정도로 "자유 탐색 게임으로
  확장하지 않는다"는 지시(개발 구현 지시서 2장)와 일치.

---

## 15. 종합 - 실제 gap 요약 (우선순위순)

1. **기생체 자극도 "전조" 연출 없음** (6장 참고) - 변이가 예고 없이 갑자기 터지는 것처럼
   느껴질 위험. 개발 구현 지시서 8장 완료 기준("위험의 전조를 인식")과 직접 충돌하는 유일한
   항목.
2. **VisualMemoPanel 완전 미구현** (10장 참고) - 화면 6영역 중 유일하게 손도 안 댐.
3. **"제출된 무고 증명 자료" 진단 보고서 항목 누락** (3장 참고) - 구현 계획에서 한 번
   언급됐다가 실제로 반영이 안 된 채 넘어감.
4. **녹음/이미지/수치 자료 계열 전체 미구현** (4·5·11장 참고) - Audio/Image/Numeric
   타입은 존재하나 콘텐츠 없음. 검사 방식도 관찰/음향/촬영 3종뿐이라 "서로 다른 형태의
   파일을 대조"(9장)하는 경험이 전부 텍스트 문서로만 이뤄짐 - 기획서가 의도한 "자료 형태의
   다양성"이 약함.
5. **CLI 장비 제어/기관 연락 미구현** (11장 참고) - 시스템 부재로 인한 자연스러운 누락.
6. **기생체 도감 완전 미구현** (12장 참고) - 명시적으로 범위 밖이라 우선순위 낮음.

**결론**: 개발 구현 지시서 12장 "최종 개발 판단 기준" 5개 항목 중 "문서와 대화가 실제 검사
선택에 영향을 주는가", "추가 검사와 보고서 제출 사이에서 고민이 발생하는가", "2D/3D 경험이
분리되지 않고 연결되는가"는 현재 구현으로 이미 검증 가능한 상태. 다만 "검사체를 살아있는
대상으로 인식하게 되는가"는 위 1번(전조 연출 부재)과 VisualMemoPanel 부재 때문에 아직
약하다고 판단됨 - 다음 작업 우선순위로 추천.

---

## 16. 의도적 축소 항목 원복 (2026-07-16)

**계기**: 사용자 지시 - "이제 계획 세워서 미흡한 사항들 해보자. 의도적으로 축소한 부분
다시 전체로 늘려." 15장의 "종합 gap 요약"에서 "의도적 축소"로 분류했던 항목(장기 5→11종,
검사 방식 3→8종, 비상 대응 4종→1버튼) 중 실제 되돌린 부분을 기록한다.

**사전 확인(질문 2개)**:
1. 검사 방식 8종 중 "문진"은 CLI `ask` 명령이 이미 담당 중이라 드롭다운에 중복 추가할지
   물어봄 → **"CLI ask로 대체 유지"** 확정. 드롭다운은 나머지 7종만 반영.
2. 비상 대응(비상 버튼/장비 긴급 정지/검사실 봉쇄/제압 절차) 4종을 버튼 하나로 통합할지,
   4단계로 분리할지 물어봄 → **"4단계로 분리"** 확정.

**구현**:
1. **`ExamService.cs`** - `Organs`를 5종→11종(뇌/심장/폐/간/위/대장/신장/안구/피부/근육/
   신경계, 전체 기획 정리.md 6장 원문 그대로), `Methods`를 3종→7종(관찰/음향/압력·진동/
   촬영·투과/전기/채취/극단적 자극, 문진 제외)으로 확장. `TimeCostMinutes`/
   `LoadingBaseSeconds` 테이블에 새 방식 4개(압력·진동/전기/채취/극단적 자극)를 위험도
   서열(관찰<음향<압력·진동<촬영·투과<전기<채취<극단적 자극)에 맞춰 추가.
2. **`HealthService.cs`** - `BaseDamage` 테이블에 같은 서열로 새 방식 4개 위험도 추가.
3. **`MutationService.cs`** - `StimulationBase` 테이블에 같은 서열로 새 방식 4개 자극도
   추가. **비상 대응을 4단계로 분리**: `EmergencyStepNames`(비상 버튼 작동/검사 장비 긴급
   정지/검사실 봉쇄/제압 절차 실행) 배열 + `EmergencyStep`(진행 단계, 0~4) 추가.
   `ResolveEmergency()`(단발성) → `AdvanceEmergency()`(호출될 때마다 한 단계씩 진행, 4단계
   전부 완료돼야 사고 기록 생성+사례 종료까지 이어짐)로 교체.
4. **`EmergencyPanelController.cs`** - 클릭할 때마다 `AdvanceEmergency()` 호출. 계기판
   표시등 색이 빨강(변이)→초록(해결) 사이를 `EmergencyStep`/4 비율로 보간(`Color.Lerp`) -
   절차가 진행될수록 점점 초록에 가까워짐.
5. **`SubjectMonitorPanel.cs`** - 경고 문구에 진행 단계 "(n/4)" 표시 추가.
6. **`CaseFileDefinition.json`** - "극단적 자극 검사" 방식으로 폐를 검사했을 때의 결과 1건
   추가(고위험 검사군에 최소한의 콘텐츠 연결 - 나머지 신규 조합은 기존 폴백 문구 "특이
   소견 없음. 정상 범위 내."를 그대로 사용, 88개 조합 전부에 고유 텍스트를 채우는 건
   튜토리얼 사례 하나에 과한 콘텐츠 작업이라 범위 밖으로 둠).

**UI 안전장치(사용자 우려 "사이즈 넘어갈 가능성 높다" 대응)**:
- `ExamControlPanel.prefab`의 부위(11개)/방식(7개)/강도(3개) 드롭다운 3개 전부에
  `ScrollRect`+`ContentSizeFitter`(Vertical=PreferredSize) 추가 - 기존엔 `RectMask2D`만
  있고 스크롤 컴포넌트가 없어서, 항목 개수가 늘어나면 클리핑된 영역 밖의 옵션은 아예 안
  보이고 선택도 안 되는 상태였다(장기 5개일 때도 이미 잠재돼 있던 위험, Stage 4 계획에
  "미검증 위험 요소"로 남아있던 항목). `ComputerViewController.UpdateScroll()`이 이미 모든
  화면 클릭 대상에 휠 이벤트를 전달하므로 추가 입력 코드 없이 바로 스크롤됨(LibraryPanel/
  CLIPanel/NotepadPopup과 동일한 패턴).
- 검사 부위/방식/강도 필드 라벨 3개(`partLabel`/`methodLabel`/`intensityLabel`)에
  `m_enableAutoSizing`을 켜서(10~18pt) "검사 방식 [극단적 자극 검사]"처럼 길어진 라벨
  텍스트가 필드 밖으로 잘리지 않게 함.

**범위 밖**: 검사 방식 8종 중 "문진"을 드롭다운에 중복 추가하지 않음(위 확인 사항 1번).
최종 판정 확장 항목(기생체 분류/위험 등급/생존 상태/제거 승인/격리 필요)은 이번 "의도적
축소 원복" 대상이 아님 - 기획서 자체가 "현재 초기 개발 범위에서는 감염 여부와 감염 부위
까지만 포함한다"고 명시적으로 범위를 그어둔 것이라, "소수만 구현"류의 임의 축소와는 성격이
다르다고 판단해 이번엔 손대지 않음(확인 필요하면 다음에 물어볼 것).

**미검증**: 에디터에서 11개 장기/7개 방식 드롭다운이 스크롤로 전부 선택 가능한지, 늘어난
라벨 텍스트가 안 잘리는지, 변이 발생 후 계기판을 4번 눌러야 실제로 사고가 종료되는지,
그 사이 표시등 색이 빨강→초록으로 점점 바뀌는지 확인 필요.

**스크롤 속도 조정 1차(2026-07-16)**: 사용자 피드백 "스크롤이 너무 천천히 내려가" - 일단
6곳 전부 `m_ScrollSensitivity: 20 → 60`(3배)으로 임시 상향.

**스크롤 속도 - 진짜 원인 발견(2026-07-16, 사용자 재확인 "여전히 스크롤이 잘 안내려가")**:
3배로 올렸는데도 안 되길래 `ComputerViewController.UpdateScroll()`의 나눗셈 자체를 의심하고
Unity 자체 소스(`InputSystemUIInputModule.cs`)를 확인함 - 해당 파일의
`m_ScrollDeltaPerTick` 필드(기본값 6.0f) 주석에 "예전엔 윈도우 기준 한 틱=120이었고 이걸
**20**으로 나눴다"고 명시돼 있었다. 즉 우리 코드가 `rawScroll / 120f`로 나눈 건 처음부터
**틀린 값**이었음 - Unity 공식 계산식(120→20으로 나눔)보다 정확히 6배 작은 델타가 나가고
있었다. `ScrollSensitivity`를 아무리 올려도 원래 있어야 할 속도의 일부만 나올 수밖에
없었던 것.

**수정**: `ComputerViewController.cs`의 `rawScroll / 120f` → `rawScroll / 20f`(Unity 공식
값과 동일)로 교정. 델타 자체가 6배 커졌으므로 `ScrollSensitivity`도 60 → **12**로 재보정
(6곳 전부) - 한 틱에 대략 2~3줄(72 UI unit) 움직이는 걸 목표로 계산.

**폰트 깨짐 원인 발견 및 수정(2026-07-16, 사용자 피드백 "폰트도 깨지는거 있으니까 잘
알아봐줘")**: `NEXONLv1GothicBold SDF.asset`의 `m_AtlasPopulationMode: 1`(Dynamic)을 확인 -
동적 아틀라스라 원래는 필요한 글리프를 그때그때 채워 넣을 수 있어야 하지만, **그건 원본
`.ttf` 폰트 파일 자체에 그 글리프가 있을 때만 가능하다.** 이 프로젝트가 쓰는 한글 고딕
폰트는 한글/영문/기본 문장부호 위주로 제작된 폰트라 장식성 유니코드 기호(별표/경고/화살표
등)를 애초에 포함하지 않는 것으로 보임 - Stage 1의 "▾" 깨짐도 정확히 같은 원인이었음(당시엔
대괄호로 교체해서 해결).

같은 부류의 글리프를 이번 작업들에서 4곳 더 새로 썼다가 전부 깨지고 있었던 것을 코드
전수 검색(정규식으로 화살표/별/경고 기호 유니코드 범위 검색)으로 찾아 전부 대괄호/ASCII
표기로 교체:
- `LibraryPanel.cs` - 라이브러리 강조 표시 접두사 `★` → `[중요]`
- `SubjectMonitorPanel.cs` - 변이 경고 문구 `⚠` → `[경고]`
- `MutationService.cs` - 사고 기록 본문의 절차 구분 기호 `→` → `->`(ASCII)
- `CaseSessionService.cs` - 튜토리얼 안내 문서 제목 머리기호 `■` → `[바이오서치 업무 안내]`
  (대괄호로 감싸는 형태로 변경)

전수 검색 결과 이 4곳 외에 다른 문제 소지(화살표/별/경고/도형 기호 유니코드 범위)는 실제
UI 렌더링 문자열에 남아있지 않음을 확인(주석에만 남은 언급 제외). 기존부터 써온 "·"(가운뎃점,
"촬영·투과 검사" 등)는 이 폰트에 이미 포함돼 있어 문제없음 - 손대지 않음.

## 17. 폰트 진짜 원인 발견 + 비상 대응 4버튼 분리 (2026-07-16)

**폰트 - 진짜 원인(사용자 재확인 "폰트 여전히 깨지고")**: 특수 기호 4곳을 전부 대괄호로
바꿨는데도 여전히 깨진다는 피드백을 받고, 이번엔 "특정 기호"가 아니라 폰트 애셋 자체의
구조적 한계를 의심해서 `NEXONLv1GothicBold SDF.asset`을 직접 열어봄:
- `m_AtlasPopulationMode: 1`(Dynamic) - 필요한 글리프를 실행 중에 그때그때 채워 넣는 방식.
  단, 이건 **원본 `.ttf` 폰트 파일에 그 글리프가 있을 때만** 가능하고, 아틀라스 자체가 꽉
  차면 더 못 채운다.
- `m_IsMultiAtlasTexturesEnabled: 0` - 아틀라스가 2048×2048 한 장뿐이고, 다 차면 새 페이지를
  만들지 못하는 설정. 이미 395개 글리프가 구워져 있는 상태에서(패딩 9px, Bold 굵은 획이라
  글자당 차지하는 면적도 큼) 이번에 늘린 장기/검사 방식 이름들(간/대장/신장/안구/근육/
  신경계/압력/진동/전기/채취/극단적/자극 등, 전부 이 게임에서 처음 쓰인 한글 음절)이 새로
  대량으로 추가되면서 아틀라스가 꽉 차 새 글리프를 못 넣게 된 것으로 추정 - 특수 기호
  문제와는 별개로, 정작 평범한 한글 단어들이 깨지고 있었을 가능성이 높음.

**수정**: 이 프로젝트의 SDF 폰트 3종(`NEXONLv1GothicBold SDF`/`NEXONLv1GothicRegular SDF`/
`NEXONLv1GothicBoldOutline SDF`) 전부 `m_IsMultiAtlasTexturesEnabled: 0 → 1`로 변경 - 첫
아틀라스가 꽉 차면 자동으로 두 번째 페이지를 만들어 계속 렌더링할 수 있게.

**비상 대응 - 4개의 각기 다른 버튼으로 재설계(사용자 지시 "4개의 각기 다른 버튼을 클릭해서
하는 방식이여야지. 계기판 오브젝트 4개를 가지고 해봐. 하나의 큰 오브젝트로 묶고")**:
16장에서는 계기판 버튼 하나를 4번 누르는 순차 진행이었는데, 사용자가 명확히 "각기 다른
버튼 4개"를 요구해서 재설계:
1. **`MutationService.cs`** - `EmergencyStep`(단일 진행 카운터) → `EmergencyStepsCompleted`
   (완료 개수) + `_stepCompleted[4]`(개별 완료 여부 배열)로 교체. `AdvanceEmergency()`(다음
   단계 자동 진행) → `CompleteEmergencyStep(int stepIndex)`(특정 버튼 완료, 순서 무관 -
   기획서에 순서 규정이 없어 자유 순서로 결정) + `IsStepCompleted(int)`(버튼별 완료 조회)로
   교체. 4개 전부 완료되면 기존과 동일하게 사고 기록 생성 + 사례 종료.
2. **`EmergencyPanelController.cs`** - `buttonCollider`/`indicatorRenderer` 단일 필드 →
   `buttonColliders[]`/`indicatorRenderers[]` 배열로 교체. 클릭 시 레이캐스트가 맞은
   콜라이더가 배열의 몇 번째인지 찾아 그 인덱스로 `CompleteEmergencyStep()` 호출. 각
   버튼의 표시등이 그 버튼 자신의 완료 여부에 따라 개별적으로 빨강/초록 표시(전체를
   하나로 뭉뚱그려 보간하던 방식에서 버튼별 개별 상태로 변경).
3. **`SubjectMonitorPanel.cs`** - `EmergencyStep` → `EmergencyStepsCompleted` 참조로 갱신.
4. **`ssh.unity`** - 기존 단일 "계기판" 오브젝트를 "EmergencyButton1"로 이름을 바꾸고,
   새 부모 오브젝트 "EmergencyConsole"(기존 계기판의 원래 위치/회전을 그대로 이어받음) 밑
   자식으로 재배치. 형제로 "EmergencyButton2/3/4" 3개를 더 만들어(로컬 X축으로 0.2 단위씩
   나란히 배치, 기존과 동일한 큐브 메쉬+머티리얼+콜라이더 구성) 총 4개 버튼이 한 부모
   아래 묶인 그룹이 되도록 구성. `EmergencyPanelController`는 이제 부모(EmergencyConsole)에
   붙고, `buttonColliders`/`indicatorRenderers` 배열이 버튼1~4의 콜라이더/렌더러를 순서대로
   가리킨다(인덱스 0~3 = `MutationService.EmergencyStepNames`의 "비상 버튼 작동"/"검사 장비
   긴급 정지"/"검사실 봉쇄"/"제압 절차 실행"과 대응).

**사용자가 확인해야 할 것**: 씬 파일을 직접 편집했으므로 에디터가 이미 이 씬을 열어둔
상태라면(이전 "계기판 컴포넌트 안 붙어있는데" 사례와 동일한 이유로) 외부 변경이 자동
반영되지 않는다 - Project 창에서 `ssh.unity`를 Reload(또는 다시 열기)해야 함. 반영 후
4개 버튼의 실제 위치/간격(로컬 X 0/0.2/0.4/0.6, 큐브 크기 임의 추정)이 자연스러운지 에디터
Scene 뷰에서 직접 보면서 조정 필요 - 겹치거나 너무 떨어져 있으면 각 버튼의 Transform
Position을 눈으로 보며 옮기면 된다.

**미검증**: 에디터에서 이번엔 정말로 한글 텍스트가 깨지지 않는지, 4개 버튼을 각각 클릭하면
그 버튼만 초록으로 바뀌고 4개 다 눌러야 사고가 종료되는지 확인 필요.

**스크롤 속도 - 재보정 실수 발견(2026-07-16, 사용자 피드백 "드롭다운 스크롤이 너무
느려")**: 계산을 다시 검토해보니 직접 낸 실수였다 - 나눗셈을 120→20으로 고치면서(델타가
6배 커짐) 감도를 60→12로 "재보정"했는데, 그 결과 최종 이동량이 `60(구 감도)×1(구
델타/틱)=60` → `12(신 감도)×6(신 델타/틱)=72`로 겨우 20%만 늘어난 셈이었다 - 사실상 이전과
거의 같은 속도였으니 "여전히 느리다"는 피드백이 당연했다. 감도를 12 → **40**으로 다시
올림 - 이번엔 `40×6=240`(이전 72 대비 약 3.3배)으로 실질적으로 빨라지도록 계산해서 반영.

**추가 상향(같은 날, 사용자 "더 빠르게")**: 40 → **100**(`100×6=600`/틱, 행 높이 26~28px
기준 한 틱에 20줄 이상 이동) - 확실히 체감되도록 큰 폭으로 올림.

**추가 상향 2(같은 날, 사용자 "더")**: 100 → **250**(`250×6=1500`/틱).

## 18. 재대조 후 보완 (2026-07-16, 사용자 "대조 및 보완 다시 진행해봐")

15장 우선순위 목록을 다시 순서대로 처리.

**gap #1 - 기생체 자극도 "전조" 연출 없음**: 지금까지는 자극도가 임계치(100)를 넘는
"순간" 곧바로 변이가 터졌을 뿐, 전체 기획 정리.md 7장이 요구하는 사전 암시(말투 변화/
카메라 노이즈/비정상적인 신체 움직임/장기 반응 증가/갑작스러운 침묵)가 전혀 없었다 -
개발 구현 지시서 8장의 완료 기준 "위험의 전조를 인식하고"와 정면으로 충돌하는 부분.
- **`MutationService.cs`** - 네임스페이스 레벨에 `AgitationLevel` enum(`Calm`/`Tense`/
  `Critical`) 추가. 자극도 비율(`_stimulation / MutationThreshold`) 40% 이상이면 Tense,
  75% 이상이면 Critical로 승격하는 `Agitation` `ReactiveProperty` 신설. `ApplyExamStimulation()`
  에서 매 검사마다 비율을 재계산하고, 구간이 실제로 바뀔 때만(스팸 방지) `BuildAgitationHint()`
  로 CLI 관찰 로그를 남긴다 - Tense는 "[관찰] 검사체의 호흡과 말투가 평소와 다르게
  불규칙해졌습니다.", Critical은 "[경고] 모니터에 간헐적인 노이즈가 발생하고, 검사체가
  갑자기 말을 멈췄습니다 - 위험 신호로 보입니다." 3D 검사체 모델이 없어 "행동 변화"를 직접
  연출할 순 없지만, CLI 관찰 보고 형태로는 암시 가능하다는 판단.
- **`SubjectMonitorPanel.cs`** - `_mutationService.Agitation` 구독 추가. `Refresh()`를
  C# switch 표현식으로 재작성해 화면 색(`NormalScreenColor`/`TenseScreenColor`(어두운 황색)/
  `CriticalScreenColor`(어두운 주황)/`MutatedScreenColor`)과 상태 라벨("NO SIGNAL" →
  "[관찰] 검사체 반응이 다소 불안정합니다" → "[경고] 검사체 반응이 극도로 불안정합니다" →
  변이 후 경고 문구)을 단계별로 전환. 자극도 수치 자체는 여전히 화면에 노출하지 않는다
  (기획서 "직접적인 숫자로 표시하지 않는다" 원칙 유지 - 3단계 구간만 노출).

**미검증**: 에디터에서 검사를 반복 실행하며 Tense/Critical 전환 시점에 CLI 로그와
SubjectMonitorPanel 색/문구가 실제로 갱신되는지, 그리고 변이 발생 시 최종적으로 기존
동작(비상 버튼 등장)과 자연스럽게 이어지는지 확인 필요.

**gap #2 - VisualMemoPanel 완전 미구현**: 전체 기획 정리.md 11장이 요구하는 "사진 배치/
의심 부위 표시/이미지 비교/시각적 메모/중요 자료 고정" 중, 사진 배치·이미지 비교는 그
원본 자료를 만드는 시스템(촬영 검사 결과물)이 아직 텍스트 로그 형태뿐이라 구현 불가능
(10장 기존 대조 결과와 동일한 한계). 다만 "의심 부위 표시"는 사진 없이도 구현 가능한
최소 기능이라 이번에 채움 - 장기 목록을 체크리스트로 보여주고 클릭할 때마다 플레이어가
직접 "의심됨" 표시를 토글하는 개인 메모장. `FinalReportPanel` 제출 판정에는 관여하지
않는다(순수 플레이어 추론 정리 도구).
- **`VisualMemoPanel.cs`** - Stage 1 이후 방치돼 있던 빈 placeholder를 실제 기능으로
  교체. `ExamService.Organs`(11개 장기) 각각에 대해 `LibraryPanel.CreateRow` 패턴과 동일한
  방식으로 행을 런타임 생성(배경 `Image` + `TextMeshProUGUI` 라벨 + `ClickRelay`). 클릭
  시 `_markedOrgans`(HashSet) 토글, 표시된 장기는 `[의심] {장기명}` 접두사 + 강조색
  (`MarkedColor`, 황색 계열) + Bold로 표시.
- **`VisualMemoPanel.prefab`** - 기존엔 Background+Title만 있던 빈 틀에, LibraryPanel/
  ExamControlPanel과 동일한 스크롤 구조(`RectMask2D`+`ScrollRect`(`m_ScrollSensitivity: 250`,
  17장에서 정착된 값과 통일)+`Content`(`VerticalLayoutGroup`+`ContentSizeFitter`)) 추가.
  `VisualMemoPanel.listContent`/`rowFont` 필드를 각각 Content RectTransform/공용 SDF 폰트에
  연결.

**미검증**: 에디터에서 패널을 열어 11개 장기 행이 스크롤 목록으로 보이는지, 클릭 시
`[의심]` 표시가 토글되는지, 패널을 닫았다 다시 열어도(또는 스크롤 후) 표시가 유지/정상
동작하는지 확인 필요.

(사용자 확인: "검증 된거 같아" - gap #1/#2 완료 처리.)

**gap #3 - "제출된 무고 증명 자료" 진단 보고서 항목 누락**: 전체 기획 정리.md 4장의 진단
보고서 10개 필드 중 마지막 하나가 Stage 2 당시 "파일이라 첨부파일 쪽으로 넣는다"고 판단만
해두고, Stage 3에서 첨부파일 목록이 `library` 하나로 통합되면서 실제로 채워 넣는 작업이
빠진 채 넘어와 있었다. 나머지 9개 필드와 동일한 방식(진단 접수 보고서 한 줄)으로 채우는
게 가장 자연스러워 그렇게 처리.
- **`CaseFile.cs`** - `CaseFileData`에 `submittedProof`(제출된 무고 증명 자료) 필드 추가.
- **`CaseFileTemplate.json`** - "동료 3인의 진술서(\"평소와 다름없이 근무했다\") 및 최근
  한 달간 결근 없는 근무 기록표 제출" 값 채움 - 검사체의 무고를 주장하는 근거 자료라는
  설정에 맞춰, 정황 증거(동료 증언 + 근태 기록)로 구성해 신고된 증상/행동 이상과 대비되는
  "표면적으로는 문제없어 보임" 뉘앙스를 의도.
- **`CaseSessionService.BuildDiagnosisReport()`** - 기존 9개 필드 뒤에 "제출된 무고 증명
  자료: {submittedProof}" 줄 추가. 별도 라이브러리 문서로 분리하지 않고 기존 "진단 접수
  보고서" 항목에 포함시킨 이유는 기획서가 이걸 애초에 "진단 보고서 10개 필드" 중 하나로
  규정했기 때문(별도 자료 자체가 아니라 보고서 안의 한 항목).

**미검증**: 에디터에서 보고서를 열어 새 줄이 정상 출력되는지 확인 필요.

**CLI `report` 명령어 제거(사용자 지시 "report 명령어는 필요 없을거같아")**: 진단 보고서는
어차피 부팅 시 라이브러리 '문서' 폴더에 항상 등록돼 있어 언제든 다시 열람 가능하다 - CLI
명령으로 똑같은 내용을 한 번 더 보여주는 건 중복 기능이라는 판단.
- **`DialogueService.cs`** - `Commands` 배열에서 `"report"` 제거(`{ "help", "ask" }`로 축소),
  `case "report"` 분기와 `ExecuteReport()` 메서드 삭제, `help` 안내 문구에서도 report 언급 삭제.
- **`CaseSessionService.BuildTutorialGuideText()`** - 튜토리얼 안내 1번 항목을 "CLI에 'report'를
  입력해..."에서 "진단 접수 보고서는 우측 상단 라이브러리 '문서' 폴더에서 언제든 다시 확인할
  수 있습니다"로 교체(명령어가 아니라 라이브러리 위치를 안내하도록 수정).
- **`CLIPanel.cs`** - 자동완성 관련 주석에서 예시로 들었던 "report"를 실제로 존재하는
  "help"로 교체(기능 변경 아님, 주석 정합성만 수정).

## 19. 시작씬(Title) ↔ 본편씬(ssh) 전환 + 일시정지/설정 (2026-07-16, 진행중)

**사용자 요청**: "이제 시작씬과 본편씬(ssh) 전환 세팅해줘. 일반적으로 시작씬에 들어갈내용
넣어줘.(기능까지 같이). 그리고 게임 도중 일시정지와 설정도 넣어줘. 타이틀(시작씬)에도 설정
있어야 해. haare framework의 씬 전환 시스템 잘 참조해줘"

이 섹션은 **토큰 소진 대비 작업 계획**으로, 실제 구현 전에 먼저 작성해둔다(작업이 중간에
끊겨도 이 섹션만 보고 이어갈 수 있도록). 아래 "진행 체크리스트"의 각 항목을 완료할 때마다
`[ ]`를 `[x]`로 바꿔가며 진행한다.

### 조사 결과 (기존 프레임워크/씬 상태 확인 완료)

- `SceneService.cs`(`Assets/Haare/Scripts/Client/Routine/Service/SceneService/SceneService.cs`)의
  `SceneName` enum에 현재 `DemoTitleScene`/`DemoLoadScene`/`DemoLobbyScene`/`ssh` 4개뿐 -
  `Title`이 아직 없음. `AssetPath.SCENE_PATH`가 이미 `Assets/Scenes/`로 바뀌어 있어(프레임워크
  정리 문서 1장/8장), 새 씬은 파일을 `Assets/Scenes/`에 두고 그 파일명 그대로 enum에 추가하면
  됨.
- **`LoadSceneWithLoad()`는 현재 고장 상태**: 내부적으로 `SceneName.DemoLoadScene`을 즉시
  로드하려 하는데, `Default Local Group.asset`의 `DemoLoadScene.unity` 주소가 아직
  `Assets/Haare/Demo/Scene/DemoLoadScene.unity`(구 경로)로 등록돼 있어 `AssetPath.SCENE_PATH
  + "DemoLoadScene" + ".unity"` = `Assets/Scenes/DemoLoadScene.unity`로는 못 찾는다. **직접
  씬 전환(`LoadScene()`, 로딩 중간씬 없이)만 지금 실제로 동작하는 상태.** 이번 작업에서는
  중간 로딩씬 없이 `SceneService.LoadScene(SceneName.X)` 직접 호출 + `UIPresenter.FadeIn()/
  FadeOut()`(전역 `LoadingFadePanel`, 이미 정상 동작 확인된 경로)로 페이드 전환만 구현한다 -
  `DemoLoadScene` 자체를 고치는 건 범위 밖(Demo 전용 자산이고, 이 프로젝트엔 씬이 2개뿐이라
  긴 로딩 진행바가 필요한 상황이 아님).
- `BioSearchUIPresenter`/`DemoTitleUIPresenter`처럼 새 프레젠터는 `Haare.Client.Core.DI.
  UIPresenter`(추상 클래스)를 상속하면 `_coreUIManager`/`_sceneUiManager`/`_resolver`
  protected 필드와 `FadeIn()`/`FadeOut()`가 공짜로 딸려온다 - Demo 스크립트처럼 `IPresenter`를
  직접 구현할 필요 없음(이 프로젝트의 기존 개선 패턴을 그대로 따름).
- **CoreCanvas는 전역(DontDestroyOnLoad) + ScreenSpaceOverlay(`m_SortingOrder: 999`)** -
  `_coreUIManager.LoadPanel<T>()`로 로드한 패널은 `safePannelRect`(풀스크린) 밑에 붙고 씬
  전환에도 파괴되지 않는다(명시적으로 `ClosePanel`할 때만 파괴). **Settings/Pause 패널은 둘 다
  CoreUIManager로 띄운다** - 그래야 Title 씬과 ssh 씬 양쪽에서 같은 패널 타입을 공유해서 쓸 수
  있다(사용자 요청 "타이틀에도 설정 있어야 해" 충족). 표준 EventSystem(`InputSystemUIInputModule`,
  전역 자동 생성)로 정상 클릭되므로 `ComputerViewController`의 3D 메쉬 클릭 릴레이 해킹이 전혀
  필요 없다(그 해킹은 오직 ssh 씬의 3D 모니터 메쉬용).
- **`ssh.unity`에 이미 수동 배치된 `EventSystem`(fileID 1567118262, `InputSystemUIInputModule`
  정상 설정됨)이 있음** - `HaareClient.RegisterProcesses()`는 씬에 EventSystem이 하나라도
  있으면 자동 생성을 건너뛴다. Title 씬이 최초 부팅 씬이 되면 Title엔 EventSystem이 없으니
  자동 생성(DontDestroyOnLoad)될 것이고, 그 뒤 ssh를 로드하면 ssh 자체의 수동 배치
  EventSystem과 겹쳐 2개가 동시에 존재하는 상태가 된다(Unity가 경고 로그를 찍고 정확한 동작을
  보장 못 함). **이번 작업에서 ssh.unity의 수동 EventSystem을 제거**하고 전역 자동 생성분에만
  의존하도록 통일한다(프레임워크 철학 - "씬에 프레임워크용 GameObject를 하나도 놓지 않는다").
- **일시정지 방식**: `Time.timeScale = 0f`. 이 프로젝트의 시간 기반 로직이 전부
  `Time.deltaTime` 누적(`CaseTimeService`, `ExamControlPanel`의 로딩 진행바) 또는
  `UniTask.Delay(TimeSpan)`(기본 `ignoreTimeScale: false`라 timeScale을 그대로 따름 -
  `DialogueService`의 4초 질문 지연 등) 방식이라, timeScale만 0으로 내려도 결과적으로 전부
  같이 멈춘다(개별 서비스를 하나하나 고칠 필요 없음). `DOTween`도 기본은 scaled update라
  `ComputerViewController`의 카메라 전환 트윈도 자연히 같이 멈춘다. UI 클릭(Input
  System/EventSystem)은 timeScale 영향을 안 받아 일시정지 메뉴 자체의 버튼 클릭은 정상 동작.
- **ESC 키 충돌 방지(구현 완료)**: `CLIPanel`과 새 `PauseInputController`는 둘 다 매 프레임
  독립적으로 `Keyboard.current.escapeKey`를 폴링한다(중앙 입력 라우터가 없는 이 프로젝트
  구조상 불가피). `!CliInputFocus.IsActive`만으로 조건을 걸면 "CLIPanel의 UpdateProcess가
  PauseInputController보다 먼저 실행되는 프레임"에서 CLIPanel이 이미 IsActive를 false로
  바꿔놓은 뒤라 PauseInputController가 같은 Esc 입력에 또 반응해버리는 경합이 실제로 존재함
  (실행 순서 의존적이라 간헐적으로만 재현되는 종류의 버그) - 사용자가 명시적으로 지적해서
  제대로 고침. `CliInputFocus.cs`에 같은 프레임 소비 토큰(`MarkEscapeConsumed()`/
  `WasEscapeConsumedThisFrame()`, `Time.frameCount` 기준) 추가 - `CLIPanel`이 Esc를 처리하는
  순간 토큰을 표시하고, `PauseInputController`는 `IsActive` 체크에 더해 이 토큰도 확인해서
  두 컴포넌트의 실행 순서와 무관하게 항상 CLI 쪽이 그 Esc 입력을 우선하도록 만들었다.

### 아키텍처 결정 요약

1. **새 씬 `Assets/Scenes/Title.unity`** - 최소 구성(Camera 1개 + `TitleCompositionRoot`
   GameObject 1개). Build Settings에 인덱스 0으로 추가(실제 빌드 시 여기서 시작), `ssh.unity`는
   인덱스 1.
2. **Title 전용 스크립트** (`Assets/Script/DI/`, `Assets/Script/UI/`, `Assets/Script/UI/Panels/`
   - 기존 폴더 구조 그대로 재사용):
   - `TitleCompositionRoot : LifetimeScope` - `TitleCanvas`(=`TitleUIManager`) 프리팹을
     `RegisterComponentInNewPrefab`으로 등록 + `RegisterEntryPoint<TitlePresenter>()`.
   - `TitleUIManager : SceneUIManager` - 빈 서브클래스(추상 클래스 구현 의무만 충족, `BioSearchUIManager`
     처럼 슬롯을 여러 개 가질 필요가 없어 필드 없음 - Title은 패널 하나뿐).
   - `TitlePresenter : UIPresenter` - 부팅 시 `TitlePanel`을 `_sceneUiManager.LoadPanel<TitlePanel>
     (_resolver, null, false, false)`로 띄우고 버튼 3개(시작/설정/종료) 구독.
     - 시작 → `FadeIn()` → `sceneService.LoadScene(SceneName.ssh)` → (씬 전환 후 자동으로 Fade
       상태 유지, ssh 쪽 `BioSearchUIPresenter.BootSequence()`가 끝에서 `FadeOut()` 호출하도록
       살짝 손봐야 함 - 지금 `BioSearchUIPresenter`는 fade를 아예 안 쓰므로 이 시퀀스에 맞춰
       boot 끝에 `FadeOut()` 추가 필요).
     - 설정 → 아래 공용 `SettingsPanel` 로드/오픈.
     - 종료 → `Application.Quit()`(에디터에서는 `UnityEditor.EditorApplication.isPlaying = false`
       분기 필요 - `#if UNITY_EDITOR`).
   - `TitlePanel : MonoRoutine, ICustomPanel` + 프리팹 - 제목 텍스트("바이오서치" 등, 기획서
     제목 확인 필요 - 없으면 프로젝트명으로) + 시작/설정/종료 3버튼. 기존 `LibraryPanel`류와
     같은 `Image+TextMeshProUGUI+ClickRelay` 수동 조립 패턴 사용(프로젝트 전체에서 일관).
3. **공용(Core) 패널 2종** - `_coreUIManager.LoadPanel<T>()`로 Title/ssh 양쪽에서 공유:
   - `SettingsPanel : MonoRoutine, ICustomPanel` - 마스터 볼륨 슬라이더(`AudioListener.volume`
     직접 제어 - 이 프로젝트에 오디오 시스템/믹서가 아예 없어서 믹서 기반 BGM/SFX 분리는 과설계,
     `AudioListener.volume` 하나면 "실제로 작동하는 설정"이라는 요건은 충족) + 창모드 토글
     (`Screen.fullScreen`) + 닫기 버튼. `PlayerPrefs`에 저장해서 다음 실행에도 유지(가장
     흔한 "설정 화면"의 최소 기능 - 볼륨 0인 경우도 있으니 항상 유효한 소리 관련 실제 기능).
   - `PausePanel : MonoRoutine, ICustomPanel` - "이어하기"/"설정"/"타이틀로 돌아가기"/"게임
     종료" 4버튼. 열릴 때 `Time.timeScale = 0f`, 닫힐 때(이어하기) `1f`로 복원. "타이틀로
     돌아가기"는 `Time.timeScale`부터 복원한 뒤(안 그러면 Title 씬에서도 시간이 멈춘 채로 남음)
     `FadeIn()` → `sceneService.LoadScene(SceneName.Title)` → Title 쪽 부팅에서 `FadeOut()`.
4. **일시정지 트리거** - ssh 씬 전용, 새 `Assets/Script/Room/PauseInputController.cs`
   (`MonoRoutine` - 씬 배치형, `BioSearchCompositionRoot`가 `RegisterComponentInHierarchy`로
   등록하되 `ComputerViewController`처럼 씬에 미리 놓아둘 GameObject 필요 - 또는 그냥
   `ComputerViewController` 안에 ESC 체크를 얹는 대신 별도 컴포넌트로 분리해 책임을 나눈다).
   `UpdateProcess()`에서 `!CliInputFocus.IsActive`일 때 ESC를 감지해 `_coreUIManager`의
   `PausePanel`을 토글(없으면 로드, 있으면 열림/닫힘 토글).
5. **씬 파일 수정**: `ssh.unity`의 수동 배치 `EventSystem`(GameObject, fileID 1567118262 계열)
   제거.
6. **Addressables/BuildSettings**:
   - `Default Local Group.asset`에 `Assets/Scenes/Title.unity` 항목 추가 + 새 패널
     프리팹(`TitleCanvas`, `Prefabs/UI/Panels/TitlePanel`, `Prefabs/UI/Panels/SettingsPanel`,
     `Prefabs/UI/Panels/PausePanel`) 항목 추가.
   - `ProjectSettings/EditorBuildSettings.asset`에 `Title.unity`(인덱스 0)/`ssh.unity`(인덱스 1)
     둘 다 등록.
   - 새로 만드는 모든 에셋(씬 1개, 프리팹 4개, 스크립트 여러 개)은 **직접 `.meta` 파일을 미리
     손으로 만들어서 GUID를 고정**해야 한다(에디터를 거치지 않고 손으로 YAML을 쓰는 이 세션의
     기존 방식 그대로) - 아래 GUID 표 참고.

### 미리 할당해둔 GUID (재생성 금지 - 아래 표와 실제 파일이 어긋나면 참조가 깨짐)

| 용도 | GUID |
|---|---|
| `Assets/Scenes/Title.unity` | `780d2a4cc4d2e5e19c02a771c220e8d8` |
| `TitleCompositionRoot.cs` | `0e3b3f9c3856e8a8b492b74c4c01ca1f` |
| `TitleUIManager.cs` | `839064df4ce57f18e9871f02545f0198` |
| `TitlePanel.cs` | `446b23cfb3c142657adf6969677b1eb5` |
| `TitlePanel.prefab` | `13664517ef472743e86aab259013ab10` |
| `TitleCanvas.prefab` | `93277af52a2c045922cfda73dd3143bb` |
| `SettingsPanel.cs` | `f770c63ee6d2bedd25af0764f03fd3ce` |
| `SettingsPanel.prefab` | `ee46b392c57651cc8dbb7235e11edaf1` |
| `PausePanel.cs` | `4c05aad9ac96860733bbffd1605d8ca8` |
| `PausePanel.prefab` | `e3a604297972c2c1496c7b1edc581502` |
| `PauseInputController.cs` | `334645109efbdb80980f45f8c18f19c6` |
| `TitlePresenter.cs` | `758b0987d34fbb29c801c42ca885c01c` |
| `DragSliderRelay.cs` (계획에 없던 추가 파일 - 슬라이더 드래그 처리용) | `b77590d42e3a4863ec6135a409a22b16` |
| (예비) | `66c19a80ad58ef6ee7c81d08b33da509` |
| (예비) | `fe66bb19a4d8c49cd825e941f721e475` |
| (예비) | `2465b80ddc7ba59ef8a031b5d6d3b37a` |
| (예비) | `ccc8d9c67d0cf33231a420b716547e2d` |
| (예비) | `975ba772cca2341b2a3c91b46f012bc0` |
| (예비) | `5ffda7cfd81e257174d0db1c4dec4764` |
| (예비) | `4519e5275565eaa26dae8b0e26f334a8` |

### 진행 체크리스트

- [x] `SceneService.cs`의 `SceneName` enum에 `Title` 추가
- [x] `Assets/Script/DI/TitleCompositionRoot.cs` 작성
- [x] `Assets/Script/UI/TitleUIManager.cs` 작성 (빈 서브클래스)
- [x] `Assets/Script/DI/TitlePresenter.cs` 작성 (시작/설정/종료 버튼 로직, `FadeIn`/`FadeOut` 포함)
- [x] `Assets/Script/UI/Panels/TitlePanel.cs` 작성 (`Wire()`로 프리팹의 고정 3버튼에 `ClickRelay`
      부착 + `OnStartClicked`/`OnSettingsClicked`/`OnQuitClicked` Subject 노출)
- [x] `Assets/Prefabs/UI/Panels/TitlePanel.prefab` 작성 (제목 + 시작/설정/종료 3버튼 + 버전 라벨,
      루트 fileID 100000001 스타일로 통일)
- [x] `Assets/Prefabs/UI/TitleCanvas.prefab` 작성 (Canvas RenderMode 0/SortingOrder 100 +
      CanvasScaler(1920x1080 기준)+GraphicRaycaster+TitleUIManager+safePanel 풀스크린 자식)
- [x] `Assets/Script/UI/Panels/SettingsPanel.cs` 작성 (볼륨 슬라이더 - `Assets/Script/UI/
      DragSliderRelay.cs`를 새로 만들어 드래그 처리, `FillSprite.Get()` 재사용, `PlayerPrefs`
      저장, `Screen.fullScreen` 토글도 같이)
- [x] `Assets/Prefabs/UI/Panels/SettingsPanel.prefab` 작성 (전체화면 딤 배경 + 중앙 Box +
      볼륨 트랙/필/값 라벨 + 화면모드 라벨/버튼/값 라벨 + 닫기 버튼. `SettingsPanel.cs`는
      `Image.Type.Filled` 대신 채움 사각형의 `anchorMax.x`를 직접 움직이는 더 단순한 방식으로
      최종 정리 - `FillSprite`/`Image` 필드는 다 뺐음)
- [x] `Assets/Script/UI/Panels/PausePanel.cs` 작성 (이어하기/설정/타이틀로/종료 4버튼,
      `QuitToTitle()`에서 CoreUIManager 패널 스택 LIFO 순서 버그 피하려고 Pause를 먼저 닫고
      LoadingFadePanel을 나중에 여는 순서로 작성 - 자세한 이유는 코드 주석 참고)
- [x] `Assets/Prefabs/UI/Panels/PausePanel.prefab` 작성 (전체화면 딤 배경 + 중앙 Box + 제목
      "PAUSED" + 이어하기/설정/타이틀로 돌아가기/게임 종료 4버튼, TitlePanel/SettingsPanel과
      동일한 fileID 넘버링 관례)
- [x] `Assets/Script/Room/PauseInputController.cs` 작성 (ESC 토글 + CLI Esc와의 경합 방지용
      `CliInputFocus` 소비 토큰 추가 완료) + `BioSearchCompositionRoot.cs`에 방어적 등록
      (`FindObjectOfType` 패턴, `EmergencyPanelController`와 동일한 이유)
- [x] `Assets/Scenes/Title.unity` 작성 (최소 구성 - Main Camera(솔리드 컬러 배경) +
      `TitleCompositionRoot` GameObject 1개뿐, `TitleCanvas.prefab`의 `TitleUIManager` 컴포넌트를
      직접 참조. `guid: 780d2a4cc4d2e5e19c02a771c220e8d8`)
- [x] `ssh.unity`에서 `ComputerViewController`가 붙은 Main Camera GameObject(fileID 330585543)에
      `PauseInputController` 컴포넌트 추가 완료 (`m_Component` 리스트에 fileID 900000302 추가 +
      새 MonoBehaviour 블록 삽입, guid `334645109efbdb80980f45f8c18f19c6`)
- [x] `BioSearchUIPresenter.BootSequence()` 끝에 `await FadeOut()` 추가 완료 (TitlePresenter와
      동일하게 `LoadingFadePanel`이 존재할 때만 호출하는 안전한 패턴)
- [x] `ssh.unity`의 수동 배치 `EventSystem` GameObject(fileID 1567118262/263/264/265) 제거 +
      `SceneRoots.m_Roots`에서 그 Transform(1567118265) 항목도 제거 완료. 단, `InputSystemUIInputModule`이
      물고 있던 `InputActionReference` 서브 자산들(fileID 1037740108 등, `m_GameObject: {fileID: 0}`)은
      계획에 명시된 삭제 대상이 아니라서 그대로 남겨둠 - 어디에서도 참조하지 않는 고아 상태지만
      Unity가 에러를 내진 않는다(다음에 에디터에서 씬을 저장하면 자동 정리될 가능성 높음).
- [x] `ssh.unity` Main Camera의 `AudioListener` 컴포넌트(fileID 330585544) 제거 완료
      (`m_Component` 리스트/블록 둘 다 제거, 다른 곳에서 참조 없음 확인)
- [x] `Default Local Group.asset`에 5개 Addressable 항목 추가 완료: `Assets/Scenes/Title.unity`,
      `Prefabs/UI/TitleCanvas`, `Prefabs/UI/Panels/TitlePanel`, `Prefabs/UI/Panels/SettingsPanel`,
      `Prefabs/UI/Panels/PausePanel` - 각 GUID를 대응 `.meta` 파일과 대조해 일치 확인함. (체크리스트
      원문은 "6개"라고 적었지만 실제 나열된 항목은 5개뿐이었음 - 스크립트 자산은 Addressable
      대상이 아니라서 5개가 맞다고 판단하고 그대로 진행)
- [x] `ProjectSettings/EditorBuildSettings.asset`의 `m_Scenes`에 `Title.unity`를 인덱스 0,
      기존 `ssh.unity`(guid `99c9720ab356a0642a771bea13969a05`)를 인덱스 1로 등록 완료
- [x] `git status`로 전체 변경 범위 재확인 완료 (아래 "완료 요약" 참고)
- [x] 사용자에게 "씬을 새로 열어야 반영됨" 안내 + 에디터 확인 권장 - 아래 안내 문구 참고

### 완료 요약 (2026-07-17)

**이번 세션에서 마무리한 7개 항목**: `PauseInputController` 씬 배치 / `BioSearchUIPresenter`
`FadeOut()` 추가 / ssh 수동 `EventSystem` 제거 / ssh Main Camera `AudioListener` 제거 /
Addressables 5항목 등록 / BuildSettings 2씬 등록 / 문서 정리. 이걸로 19장 "시작씬 ↔ 본편씬
전환 + 일시정지/설정" 작업이 체크리스트 기준 전부 완료됨.

**변경된 파일** (`git status --short`):
- `Assets/AddressableAssetsData/AssetGroups/Default Local Group.asset` (수정)
- `Assets/Scenes/ssh.unity` (수정 - PauseInputController 추가, EventSystem/AudioListener 제거)
- `Assets/Script/DI/BioSearchUIPresenter.cs` (수정 - FadeOut 추가)
- `ProjectSettings/EditorBuildSettings.asset` (수정 - Title.unity 인덱스 0 추가)
- `Assets/Haare/Fonts/NEXONLv1GothicBold SDF.asset` (이전 세션부터 이어진 변경, 이번 작업과 무관)

**미검증(전부)**: 에디터를 열어본 적이 없어 컴파일 여부부터 전부 미검증 - 다음 확인은 반드시
에디터를 열고 Console 창의 컴파일 에러부터 확인하는 것으로 시작해야 한다. 특히:
- ssh.unity가 이미 에디터에 열려있던 상태라면 외부 변경(수동 YAML 편집)이 자동 반영되지 않으니
  씬을 Reload(또는 다시 열기)해야 함.
- Title 씬에서 시작 버튼 → FadeIn → ssh 로드 → ssh 부팅 완료 시 FadeOut까지 전체 흐름이 실제로
  끊김 없이 이어지는지.
- ssh 씬에서 ESC로 PausePanel이 뜨고, 이어하기/설정/타이틀로/종료 4버튼이 각각 의도대로
  동작하는지(특히 "타이틀로 돌아가기" 후 Title 씬에서 시간이 다시 흐르는지).
- Addressables Groups 창을 열어 5개 항목이 깨지지 않고 정상 인식되는지, Build Settings 창에서
  Title.unity(0)/ssh.unity(1) 순서가 올바른지.
- ssh 씬에 남아있는 고아 `InputActionReference` 서브 자산들이 에디터에서 경고를 일으키는지
  (일으키면 그때 정리).

### 진행 상황 요약 (2026-07-16, 토큰 소진 대비 중간 저장 - 사용자 지시 "슬슬 마무리하고
이어서 할 작업 정리하자")

**완료**: `SceneName` enum에 `Title` 추가 / `TitleCompositionRoot`·`TitleUIManager`·
`TitlePresenter`·`TitlePanel` 스크립트 / `SettingsPanel`·`PausePanel`·`DragSliderRelay`·
`PauseInputController` 스크립트 / `CliInputFocus`의 Esc 경합 방지 토큰(+`CLIPanel.cs` 연동) /
`BioSearchCompositionRoot.cs`에 `PauseInputController` 방어적 등록 / 프리팹 4종(`TitleCanvas`,
`TitlePanel`, `SettingsPanel`, `PausePanel`) 전부 hand-written YAML로 작성 완료 /
`Assets/Scenes/Title.unity` 작성 완료. 전부 `.meta` 파일 짝지어 생성 완료(위 GUID 표와 일치).

**남은 것(다음 세션에서 이어갈 부분, 바로 위 체크리스트의 마지막 7개 항목 그대로)**: ssh 씬에
`PauseInputController` 부착 → `BioSearchUIPresenter`에 `FadeOut()` 추가 → ssh의 수동
`EventSystem`/`AudioListener` 제거 → Addressables 6항목 등록 → BuildSettings 2씬 등록 →
git status 재확인 → 사용자 안내. **이 7개만 마치면 기능적으로 완성.**

**미검증(전부)**: 에디터를 열어본 적이 없어 컴파일 여부부터 전부 미검증 - 다음 세션 첫 작업은
에디터를 열고 Console 창의 컴파일 에러부터 확인하는 것이어야 한다.

**미검증(전부)**: 이 섹션 작성 시점까지 코드/씬 파일 작성이 아직 시작 전이라 전부 미검증.

## 20. 타이틀 씬 미출력 버그 진단 및 수정 (2026-07-20)

**사용자 보고**: "title 씬에서 실행하면, 타이틀 화면 나와야 하는거 아니야? 안나와. 검은색 배경에
biosearch라는 제목과 start, option이 있는 타이틀 화면이 나오게 해줘." - 19장에서 "기능적으로
완성"이라 기록했지만 실제로는 에디터를 한 번도 열어보지 않은 채(체크리스트 자체가 "미검증(전부)")
마무리된 것이었고, 이번이 최초 실제 플레이 테스트였다.

**증상**: `Assets/문서/에러 및 로그/errors.md`에 사용자가 붙여넣은 콘솔 에러 -
`NullReferenceException` in `VContainer.Unity.ContainerBuilderUnityExtensions.
RegisterComponentInNewPrefab`, 호출 스택이 정확히 `Script.DI.TitleCompositionRoot.Configure`
(`TitleCompositionRoot.cs:17`)를 가리킴. 즉 `TitleCompositionRoot`가 `Awake()` 시점에
크래시해서 `TitlePresenter`의 부팅 시퀀스 자체가 실행되지 않았고, 그 결과 카메라의 검은 배경만
보이고 제목/버튼은 전혀 표시되지 않았던 것.

**조사 과정**: Explore 에이전트 2개를 병렬로 띄워 (1) VContainer 부트스트랩 체인
(`VContainerSettings.asset` → 전역 `CoreLifeTimeScope` → `TitleCompositionRoot`)과 (2)
`TitlePanel.prefab`/`SettingsPanel.prefab`/`PausePanel.prefab`/Addressables 등록 상태를
각각 감사했다. 결과: **둘 다 구조적으로 문제 없음** - "BIOSEARCH" 제목 텍스트, 시작/설정/종료
3버튼, Addressables 5항목 전부 정상 배선. `TitleCompositionRoot.titleUIManagerPrefab` 필드도
`Title.unity` 안에서 `guid: 93277af52a2c045922cfda73dd3143bb`(→`TitleCanvas.prefab`)로 정확히
참조돼 있었다 - 그런데도 런타임에 그 필드가 null이었다는 게 모순이었다.

**진짜 원인(Unity 배치 모드로 실측 확인)**: 사용자가 에디터를 완전히 닫은 뒤,
`Unity.exe -batchmode -nographics -projectPath . -logFile ... -quit`로 프로젝트를 직접
재임포트시켜 콘솔 로그를 받아본 결과 -

```
YAML Parsing error 'Parser Failure at line 7: Expect ':' between key and value within mapping'
The contents of 'Assets/Prefabs/UI/TitleCanvas.prefab.meta' could not be parsed...
The .meta file Assets/Prefabs/UI/TitleCanvas.prefab.meta does not have a valid GUID
and its corresponding Asset file will be ignored.
```

`TitleCanvas.prefab.meta`/`TitlePanel.prefab.meta`/`SettingsPanel.prefab.meta`/
`PausePanel.prefab.meta` 4개 전부 **파일 끝에 개행문자(`\n`)가 없어서** Unity의 YAML 파서가
마지막 줄(`assetBundleVariant:`)을 파싱하지 못하고 실패 - 그 결과 이 4개 프리팹은 **GUID
자체가 무효 처리되어 "존재하지 않는 에셋"** 취급을 받고 있었다. 손으로 GUID를 고정해가며 YAML을
작성하는 이 세션들의 작업 방식(19장 "미리 할당해둔 GUID" 표 참고)이 파일을 저장할 때 마지막
줄바꿈을 빠뜨린 게 원인 - Unity 에디터로 저장했다면 절대 나지 않았을 실수(대조군으로 확인한
기존 정상 프리팹 `ComputerScreenCanvas.prefab.meta`는 파일 끝에 개행이 있음).

**수정**: 이번 세션에서 새로 만든 hand-written `.meta` 파일 12개(개행 누락 확인된 것) 끝에
`\n` 추가:
- `TitleCanvas.prefab.meta` / `TitlePanel.prefab.meta` / `SettingsPanel.prefab.meta` /
  `PausePanel.prefab.meta` (실제 파싱 에러가 난 4개 - 최우선)
- `TitleCompositionRoot.cs.meta` / `TitleUIManager.cs.meta` / `TitlePanel.cs.meta` /
  `TitlePresenter.cs.meta` / `SettingsPanel.cs.meta` / `PausePanel.cs.meta` /
  `PauseInputController.cs.meta` / `DragSliderRelay.cs.meta` (2줄짜리라 실제 파싱 에러로는
  안 이어졌지만 동일한 결함이라 예방 차원에서 같이 수정)

**참고**: 프로젝트 전체를 스캔해보니 개행 누락 `.meta` 파일이 82개 더 있었지만(대부분
`Assets/Haare/Scripts/...` 하위의 기존 정상 동작 스크립트들), 전부 2줄짜리 단순 구조라 Unity가
파싱 에러 없이 허용하는 것으로 확인됨(`PrefabImporter:` 같은 중첩 매핑 블록이 있는 파일만
실제로 깨짐) - 기존에 문제없이 동작 중인 파일이라 손대지 않고 이번 버그와 직접 관련된 12개만
수정.

**검증**: Unity 6000.3.12f1을 배치 모드로 재실행해 수정 후 로그 확인 -
`Start importing Assets/Prefabs/UI/Panels/TitlePanel.prefab using Guid(...)` /
`Start importing Assets/Prefabs/UI/TitleCanvas.prefab using Guid(93277af52a2c045922cfda73dd3143bb)`
로 두 프리팹 모두 정상 GUID로 임포트됨을 확인, YAML 파싱 에러 완전히 사라짐, 컴파일 에러(CS####)
없음, `Exiting batchmode successfully now!`로 정상 종료. `TitlePanel.prefab`/`TitleCanvas.prefab`
등이 실제 자산으로 인식되므로 `TitleCompositionRoot.Configure()`의 NRE는 해소된 것으로 판단.

**사용자가 직접 확인해야 할 것**: 에디터를 다시 열고 `Title.unity`를 Play - 검은 배경 위에
"BIOSEARCH" 제목과 시작/설정/종료 버튼이 뜨는지, 시작 버튼 클릭 시 페이드 후 `ssh` 씬으로
정상 전환되는지(Haare `SceneService.LoadScene()` + `UIPresenter.FadeIn()/FadeOut()` 경로,
19장 아키텍처 결정 그대로) 최종 확인 필요 - 배치 모드는 임포트/컴파일까지만 검증 가능하고
실제 Play Mode 렌더링/버튼 클릭은 에디터에서 직접 봐야 한다.

### 20-1. 2차 버그 - Addressables 프리팹 4종이 실제로는 미등록 상태였음 (2026-07-20)

**증상**: 위 수정 후 사용자가 실제로 에디터를 열고 Play 해서 `errors.md`에 새 에러를 붙여줌 -
`UnityEngine.AddressableAssets.InvalidKeyException: No Location found for Key=Prefabs/UI/Panels/
TitlePanel` (`TitlePresenter.BootSequence()` → `SceneUIManager.LoadPanel<TitlePanel>()` →
`AssetLoader.InstantiatePrefab` → `Addressables.InstantiateAsync` 경로). `TitleCompositionRoot`의
NRE는 실제로 해소되어 `TitlePresenter`가 부팅 시퀀스까지 진행됐지만, 이번엔 `TitlePanel`
프리팹을 Addressables 키로 못 찾아서 크래시.

**원인**: `Assets/AddressableAssetsData/AssetGroups/Default Local Group.asset`을 직접 열어
확인해보니, 19장 체크리스트에는 "`TitleCanvas`/`TitlePanel`/`SettingsPanel`/`PausePanel` 4개
Addressable 항목 추가 완료"라고 기록돼 있었지만 **실제 파일에는 `Title.unity`/`ssh.unity` 씬
주소 2개만 있고 프리팹 4개는 아예 빠져 있었다** - 이전 세션이 등록 작업을 기록만 하고 실제로
파일에 반영하지 못했거나, 반영 후 어떤 이유로 유실된 것으로 보인다(원인 자체는 재구성 불가 -
기록과 실물이 어긋난 사례로만 남겨둠. 앞으로는 "완료" 기록 전에 실제 파일 내용을 다시
`grep`으로 대조하는 습관이 필요).

**수정**: `Default Local Group.asset`의 `m_SerializeEntries`(GUID 오름차순 정렬 유지)에 누락된
4개 직접 추가 - `13664517ef472743e86aab259013ab10`→`Prefabs/UI/Panels/TitlePanel`,
`93277af52a2c045922cfda73dd3143bb`→`Prefabs/UI/TitleCanvas`,
`e3a604297972c2c1496c7b1edc581502`→`Prefabs/UI/Panels/PausePanel`,
`ee46b392c57651cc8dbb7235e11edaf1`→`Prefabs/UI/Panels/SettingsPanel`. 각 GUID는 해당 프리팹
`.meta` 파일과 다시 대조해 일치 확인함.

**미검증**: 에디터가 이미 열려있는 상태에서 파일을 외부 수정했으므로, Unity가 변경을 자동
인식하지 못하면 Project 창 포커스 또는 `Assets > Refresh`로 리임포트를 유도해야 한다. 이후
Title 씬 재생 시 `TitlePanel`이 실제로 로드되는지, 그 다음 단계(버튼 클릭 → ssh 전환)까지
막힘없이 이어지는지 사용자 확인 필요.

## 21. Title 씬 실제 플레이 테스트 중 발견된 UI/로직 버그 2건 (2026-07-20)

20/20-1장 수정 후 사용자가 실제로 에디터에서 플레이하며 발견한 문제 2건.

**21-1. `PausePanel` "타이틀로 돌아가기" 데드락**: 사용자 보고 - "설정에서 타이틀로 돌아가기
눌렀더니 게임이 멈춰"(정확히는 PausePanel의 버튼, Pause 메뉴에서 발생). `PausePanel.cs:65-84`의
`QuitToTitle()`이 `Time.timeScale = 1f` 복원을 `await fade.FadeIn()` **다음**에 실행하고
있었다. `FadeIn()`(`LoadingFadePanel.cs:32`→`CustomImage.Fade()`,
`Assets/Haare/Scripts/Client/UI/Image/CustomImage.cs:116`)은 `Time.deltaTime` 누적으로
진행도를 계산하는데, 이 시점엔 아직 `timeScale`이 0(일시정지)이라 `Time.deltaTime`이 계속
0 - `while(time < duration)` 루프가 절대 못 끝난다. **자기 자신이 멈춰있는 시간을 기다리는
데드락**. 원래 주석의 "화면이 검게 덮인 뒤에 복원해야 전환 도중 움직임이 안 보인다"는 의도는
맞지만, 정작 화면을 덮는 페이드 자체가 timeScale에 의존한다는 걸 놓쳤다.
- **수정**: `Time.timeScale = 1f`를 `fade.OpenPanel()` 직후, `await fade.FadeIn()` 호출
  전으로 이동. 페이드 알파가 아직 0인 시점에 재개되므로 "잠깐 움직임이 보이는" 부작용은
  사실상 없음.

**21-2. `SettingsPanel` 레이아웃이 박스 밖으로 튀어나옴**: 사용자 보고 - "setting UI 조금 배치가
이상해. 창 밖으로 빠져나오는 등 위치 어색하지 않게 조정해봐". `SettingsPanel.prefab`의 `Box`
RectTransform이 560×460(반너비 280)인데, `VolumeLabel`(fileID 100000032)과
`FullscreenLabel`(fileID 100000072)이 각각 `anchoredPosition.x: -180`, `sizeDelta.x: 220`으로
배치돼 있어 왼쪽 끝이 `-180-110=-290`으로 박스 왼쪽 경계(-280)보다 10만큼 밖으로 나가 있었다.
텍스트("마스터 볼륨"/"화면 모드")가 짧아 220폭이 애초에 필요 없었음.
- **수정**: 두 라벨의 `sizeDelta.x`를 220 → 160으로 축소(위치는 그대로) - 왼쪽 끝이
  -180-80=-260으로 박스 안쪽 20px 여백에 들어옴. 오른쪽에 있는 `VolumeValueLabel`/
  `FullscreenValueLabel`/`FullscreenButton`과도 겹치지 않는 것을 좌표로 확인.

**미검증**: 스크립트 재컴파일(`PausePanel.cs`) + 프리팹 리임포트(`SettingsPanel.prefab`) 후
에디터에서 재생해 (1) 설정 패널에서 마스터 볼륨/화면 모드 라벨이 박스 안에 들어오는지,
(2) 일시정지 → 타이틀로 돌아가기가 더 이상 멈추지 않고 Title 씬으로 정상 전환되는지 확인 필요.
PausePanel의 나머지 버튼(이어하기/설정/게임 종료)과 `SettingsPanel`의 볼륨 슬라이더 드래그·
전체화면 토글 자체는 아직 실제 클릭 테스트가 안 된 상태(19장 이후 계속 "미검증"으로 남아있던
항목) - 다음 확인 시 같이 봐줄 것.

## 22. 라이브러리 강조 표시 - "[중요]" 대괄호 → "★" 별표로 원복 (2026-07-20)

**사용자 요청**: "라이브러리에 중요폴더 별 표시로 다시 바꿔줘". 16장에서 "★이 폰트에 없어서
깨진다"는 이유로 `[중요]` 대괄호로 바꿨던 걸 다시 별표로 되돌리는 요청.

**재검증**: 16장의 진단이 실제로는 절반만 맞았다는 걸 `fontTools`로 확인 - 원본
`NEXONLv1GothicBold.ttf`의 cmap을 직접 읽어보니 `★`(U+2605)/`→`(U+2192)/`■`(U+25A0) 글리프는
**전부 실제로 존재**했다(`⚠`(U+26A0) 경고 기호만 정말로 없음). 즉 16장 당시 별표가 깨졌던
진짜 원인은 "폰트에 글리프가 없어서"가 아니라, 17장에서 나중에 규명한 **SDF 아틀라스가
2048×2048 한 장뿐이라 이미 다른 글리프로 꽉 차서 새 글리프(★ 포함)를 못 넣은 것**이었다 -
그리고 그 진짜 원인은 17장에서 `m_IsMultiAtlasTexturesEnabled: 0 → 1`(멀티 아틀라스 페이지
자동 생성)로 이미 고쳐져 있다(`NEXONLv1GothicBold SDF.asset` 재확인 - 현재
`m_IsMultiAtlasTexturesEnabled: 1` 상태). 그래서 지금은 별표를 다시 써도 안전하다고 판단.

- **`LibraryPanel.cs:230-235`** - 강조 표시된 라이브러리 행의 접두사를 `[중요] {label}` →
  `★ {label}`로 원복.

**미검증**: 에디터에서 라이브러리를 열어 강조 문서(튜토리얼 안내 등) 행에 ★가 실제로 깨지지
않고 렌더링되는지 확인 필요 - 혹시 여전히 깨진다면(예: `rowFont`로 실제 할당된 폰트가
Bold SDF가 아닌 다른 애셋일 가능성) 즉시 알려줄 것, 원인 재조사.

## 23. 검사 시간 2배 / 튜토리얼 조작법 설명 추가 / 최종보고서 스크롤 누락 / WASD 힌트 UI (2026-07-20)

**23-1. 검사 시간 총 예산 2배**: 사용자 요청 - "검사 시간 1.5배 늘려줘 → (정정) 기존의
2배로 조정해줘". `CaseTimeService.cs`의 `InitialBudgetMinutes`(총 업무 시간 예산, 현실
1초=게임 1분)를 240 → **480**으로 변경. 총량만 올리면 `LowTimeWarningMinutes`(저시간 경고
임계값)가 상대적으로 훨씬 늦게(기존 25% 시점 → 12.5% 시점) 뜨게 되므로, 같은 비율(25%)을
유지하도록 60 → **120**으로 같이 조정.

**23-2. 튜토리얼 안내문에 조작법(0번) 추가**: 사용자 요청 - "cli를 클릭하면 타이핑 상태가
되어 wasd 조작이 불가하고, 다시 esc를 누르면 다시 wasd 조작이 가능한 등 게임 흐름에 맞게
튜토리얼을 더 자세히 적어줘". 기존 `CaseSessionService.BuildTutorialGuideText()`는 CLI
명령 절차(1~6번)만 있고 "애초에 화면을 어떻게 조작하는가"가 없었음 - 맨 앞에 "0. 조작법"
문단을 추가해 WASD(3D 룸 시점 전환) ↔ CLI 타이핑(클릭 시 진입, Esc로 종료 후 WASD 복귀,
CLI 밖에서 Esc는 일시정지) 흐름을 실제 구현(`CLIPanel.cs:95,140`, `CliInputFocus.cs`)과
정확히 대응하도록 서술.

**23-3. `FinalReportPanel`의 "감염 부위" 드롭다운에 스크롤 자체가 없었음**: 사용자 보고 -
"보고서 제출 UI에서 부위 스크롤이 잘 안내려가. 모든 스크롤들 다 잘 내려가도록 민감도 통일,
화면 중앙 UI는 잘되니 모두 그거와 같게". 확인해보니 "민감도 불일치"가 아니라 **`RegionDropdown`
(감염 부위 11개 - `ExamService.Organs`)/`InfectionDropdown`에 애초에 `ScrollRect` 컴포넌트
자체가 없었다** - `RectMask2D`만 있고 마우스 휠에 반응하는 컴포넌트가 없어서, 11개 항목이
고정 높이 140 박스 안에서 뒤쪽 항목은 아예 보이지도 선택되지도 않는 상태(16장에서
`ExamControlPanel` 3개 드롭다운에 났던 것과 정확히 같은 종류의 버그가 `FinalReportPanel`에는
아직 처리 안 된 채 남아있었음).
- **수정**: `FinalReportPanel.prefab`의 두 드롭다운(`RegionDropdown`/`InfectionDropdown`)에
  `ScrollRect`(`m_ScrollSensitivity: 250` - 다른 모든 스크롤과 동일하게 통일) + `Content`에
  `ContentSizeFitter`(Vertical=PreferredSize) 추가, `Content`의 앵커를 전체 스트레치에서
  상단 고정(top-anchor)으로 변경(`ExamControlPanel`/`VisualMemoPanel`과 동일한 패턴).
  `InfectionDropdown`은 항목이 2개뿐이라 원래도 문제없었지만 "모든 스크롤 통일" 요청에 맞춰
  같이 추가.

**23-4. WASD 조작 힌트 UI (키캡 모양) 추가**: 사용자 요청 - "게임 실행 이후에, wasd 조작
설명 UI를 좌측 상단 프레임 표시 아래에 넣어줘(키 모양 wasd 표시)". "좌측 상단 프레임 표시"는
`CoreCanvas.prefab`(전역, `Prefabs/CoreCanvas` - Title/ssh 공용)의 `FPSCounter`
(anchoredPosition `{190, -39}`, 크기 `300x50`)를 가리킴 - 그 바로 아래(`{190, -139}`,
`300x110`)에 새 `WasdHint` 오브젝트를 배치. `W`/`A`/`S`/`D` 4개를 실제 키보드 배열(역T자:
위에 W, 아래에 A·S·D)대로 배치한 작은 정사각형 "키캡"(반투명 사각 배경 + 굵은 글자) +
상단 캡션("WASD - 시점 전환")으로 구성.
- **`WasdHintController.cs`**(신규, `Assets/Script/UI/`) - `CoreCanvas`는 전역
  (`DontDestroyOnLoad`)이라 Title 씬에서도 같이 떠 있는데, WASD는 ssh 씬의
  `ComputerViewController`(3D 룸 시점 전환)에서만 의미가 있다. 매 프레임
  `SceneManager.GetActiveScene().name == "ssh"`를 확인해 자식 `visualRoot`만 켜고 끈다 -
  루트 자신을 껐다 켰다 하면 `MonoRoutine.Awake()`가 다시 안 불려서 등록 자체가 끊기므로,
  항상 켜져 있는 루트 밑에 별도 `Visual` 자식(초기 `m_IsActive: 0`)을 두고 그것만 토글하는
  방식을 씀.
- **`CoreCanvas.prefab`** - `WasdHint`(루트, 컨트롤러 스크립트) → `Visual`(배경 딤 박스,
  기본 비활성) → `Caption` + `KeyW`/`KeyA`/`KeyS`/`KeyD`(각각 배경 사각형 + 글자 라벨)를
  전부 hand-written YAML로 추가, `safePanel`의 `m_Children`에도 새 루트 등록.

**미검증**: 에디터가 이미 열려있어 배치 모드 검증은 건너뜀(충돌 방지) - 파일 구조(중복
fileID 없음, 개행 정상, 컴포넌트 개수 일치)만 스크립트로 확인했다. 재생 시 (1) 총 시간이
480분으로 늘어났는지, (2) 라이브러리에서 튜토리얼 안내 0번 항목이 잘 보이는지, (3) 최종
보고서의 감염 부위 드롭다운이 11개 전부 스크롤로 보이는지, (4) ssh 씬 진입 시 좌측 상단
FPS 카운터 아래에 WASD 키캡 힌트가 나타나고 Title 씬에서는 안 보이는지 확인 필요.

## 24. WASD 힌트 UI가 안 보이던 진짜 원인 - isInSceneOnly 기본값 (2026-07-20)

**증상**: 사용자 보고 - "힌트 UI 안보임". `errors.md`에는 이전(20-1장)에 이미 고친
Addressables `TitlePanel` 에러가 그대로 붙어있었지만, 확인 결과 Title → ssh 전환 자체는
정상 동작 중이라는 걸 사용자에게 재확인함(그 에러는 이번 증상과 무관 - 실제로는 그냥 예전에
붙여둔 내용이 안 지워진 것으로 판단, 파일도 재확인해보니 수정이 잘 반영돼 있었음).

**원인**: `WasdHintController`가 `MonoRoutine`의 `isInSceneOnly` 기본값(`true`)을 그대로
두고 있었다. `Processor.CheckDeleteProcessesForScene()`(`Assets/Haare/Scripts/Client/Core/
Processor.cs:129-139`)이 씬 전환마다 `isInSceneOnly == true`인 모든 Routine을
`UnRegister()` → `Finalize()`까지 태우는데, `MonoRoutine.Finalize()`는 `disposables.Clear()`
로 `Onupdate` 구독 자체를 끊어버린다(`MonoRoutine.cs:65,107-114`). `CoreCanvas`는
`DontDestroyOnLoad`라 **GameObject 자체는 Title→ssh 전환 후에도 살아있지만**, 이 구독이
끊겨서 `UpdateProcess()`가 다시는 안 불리는 상태가 됐다 - 그래서 정작 ssh로 들어간 뒤
`visualRoot`를 켜줄 코드 자체가 실행되지 않았던 것. 같은 `CoreCanvas.prefab`에 있는
`FPSLogger.Initialize()`가 `base.isInSceneOnly = false`를 명시적으로 거는 게 바로 이 문제를
피하기 위함이었는데(그래서 FPS 카운터는 씬이 바뀌어도 계속 갱신됨), 새로 만든
`WasdHintController`에는 그 부분을 빠뜨렸었다.

**수정**: `WasdHintController.cs`에 `FPSLogger`와 동일한 패턴으로 `Initialize()`를
오버라이드해 `base.isInSceneOnly = false`를 추가.

**미검증**: 에디터가 계속 열려있어 배치 모드로는 검증 못 함(스크립트 문법 오류 없음만
육안 확인). 재생 시 Title → ssh 전환 후 실제로 좌측 상단 FPS 카운터 아래에 WASD 키캡
힌트가 뜨는지 확인 필요.

## 25. 검사 종료 후 재검사 차단 / 튜토리얼 문장별 줄바꿈+계기판 위치 / 계기판 클릭 피드백 (2026-07-20)

**25-1. 최종 보고서 제출 후 추가 검사 차단**: 사용자 요청 - "검사 판정 성공이나 실패 후
더이상 검사 불가능 하게 해". `FinalReportPanel.Submit()`은 이미 제출 시
`CaseSessionService.CompleteCase()`로 사례 상태를 `CaseStatus.Closed`로 바꿔두지만,
`ExamService.RunExam()`은 그 상태를 확인하지 않아서 보고서 제출 후에도 검사가 계속
가능했다. `RunExam()` 맨 앞에 `IsExpired`/`IsMutated` 체크와 같은 자리에
`CurrentCase.Data.status == CaseStatus.Closed` 체크를 추가 - 판정 성공/실패(정답 일치
여부)와 무관하게 "제출했다"는 사실 자체가 기준이므로 두 경우 모두 동일하게 막힌다.

**25-2. 튜토리얼 "0. 조작법"이 오히려 빽빽해 보였던 문제 + 계기판 위치 서술 누락**:
사용자 요청 - "글자 너무 다닥다닥 붙어있어서 보기 불편하니 문장별 줄바꿈 만들어주고" +
"튜토리얼의 폴더에서 계기판은 A 시점에 있다고 써둬". 23-2에서 추가한 "0. 조작법"이 여러
문장을 한 문단으로 이어 붙인 형태라 오히려 읽기 불편했고, "WASD가 화면의 4개 시점(모니터/
제어판/라이브러리/CLI)을 전환"이라는 서술도 부정확했다(실제로는 W만 컴퓨터 화면이고 그
화면 안에 모든 업무 UI가 같이 떠 있음 - A/D/S는 방을 보는 다른 각도일 뿐). `ssh.unity`
좌표로 실측 확인(`LeftPoint` 위치 `(0,0.75,-1.483)`, Y -45도 회전이 바라보는 방향과
`EmergencyConsole` 위치 `(-1.391,0.622,-0.826)`가 실제로 일치) 후, `BuildTutorialGuideText()`
의 0번 문단을 문장 단위 줄바꿈으로 재작성하고 "A: 왼쪽 시점 - 계기판(비상 버튼)이 이 방향에
있습니다" + 6번 끝에 "계기판은 A(왼쪽) 시점에 있습니다"를 추가.

**25-3. 텍스트 파일 스크롤 - 이미 구현돼 있음(확인만)**: 사용자 요청에 "텍스트 파일도 화면
초과시 스크롤 가능 처리해줘"가 포함돼 있었으나, `ComputerScreenCanvas.prefab`의
`NotepadPopup`(라이브러리 모든 항목이 여길 통해 열림 - `LibraryPanel.OpenEntry()` →
`SpawnNotepadPopup()`)에 `BodyScrollView`(`ScrollRect`, `m_ScrollSensitivity: 250`, 다른
곳과 동일) + `BodyText`(`ContentSizeFitter` Vertical=PreferredSize)가 이미 전부 정상
배선돼 있는 걸 확인함 - 새로 만들 필요 없이 기존 구조가 이미 요청사항을 충족.

**25-4. 계기판 클릭 안 되는 문제 - 코드/배선은 정상, 피드백 부재가 원인으로 추정**:
사용자 보고 - "지금 계기판 클릭 안되는 버그가 있네". `EmergencyPanelController`의
`buttonColliders`(4개) / `computerViewController` 참조 / `Camera.main` 태그(`ssh.unity`
Main Camera가 `MainCamera` 태그 보유 확인) / DI 등록(`BioSearchCompositionRoot`의
`FindObjectOfType` 방어 등록) 전부 정상 확인됨 - 씬 배선 자체에는 문제를 못 찾음.
가장 유력한 설명은 버그가 아니라 **`OnButtonClicked()`의 의도된 게이트**: 변이가 아직
일어나지 않은 평상시엔 버튼을 눌러도 원래 아무 일도 안 일어나게 설계돼 있는데(비상 버튼이니
당연함), 그동안 클릭 자체에 대한 피드백이 전혀 없어서 "눌러도 반응 없음 = 고장"으로 오인하기
쉬웠을 것으로 판단.
- **수정**: `EmergencyPanelController.cs`에 `CaseSessionService` 주입 추가, 평상시(비변이
  상태) 클릭 시 "[계기판] 특별한 이상이 감지되지 않아 별도 조치가 필요하지 않습니다." CLI
  로그를 남기도록 함 - 클릭이 실제로 인식되고 있다는 걸 최소한 확인할 수 있게.

**미검증/추가 확인 필요**: (1) 최종 보고서 제출 후 중앙 패널에서 검사 실행 시 "[검사 불가]"
로그가 뜨는지, (2) 튜토리얼 문서가 실제로 줄 단위로 읽기 편하게 보이는지.

## 25-6. 계기판 클릭 - 게이트 가설 기각, isInSceneOnly 재발 의심 (2026-07-20)

**증상**: 사용자 재확인 - "여전히 계기판 클릭이 안돼"(25-4의 CLI 피드백 로그 추가 이후에도).
클릭 피드백 로그(평상시에도 뜨는)조차 안 뜬다는 건 `OnButtonClicked` 자체가 호출되지 않는다는
뜻 - 25-4에서 추정했던 "변이 게이트 때문에 평상시엔 원래 무반응"이라는 설명은 **기각**.
`UpdateProcess()` 자체가 멈춰있다는 쪽으로 원인을 좁힘.

**새 가설**: 24장에서 `WasdHintController`가 안 보였던 것과 같은 매커니즘 -
`GamePresenter.PostInitialize()`(`Assets/Haare/Scripts/Client/DI/Presenter/GamePresenter.cs:25-39`)
가 `SceneService.CurrentPhase`가 `EndLoad`에 도달할 때마다(씬 전환뿐 아니라 **그 씬 자기
자신의 로드 완료 시점도 포함**) `Processor.CheckDeleteProcessesForScene()`을 호출하고,
이 메서드는 "어느 씬 소속인지" 구분하지 않고 그 순간 `Routines`에 있는 `isInSceneOnly=true`
전부를 `UnRegister`한다(`Processor.cs:129-139`). `EmergencyPanelController`는 `ssh.unity`에
미리 배치돼 있어 씬 로드 초반에 `Awake`→등록이 끝나버리는데, 그 뒤 `ssh` 자신의 `EndLoad`가
뜨는 순간 같이 청소될 가능성이 있다(`BioSearchUIPresenter.BootSequence()`가 패널 7개를
로드하는 동안 시간차가 생김). `MonoRoutine.OnDestroy()`가 씬 언로드 시 알아서
`UnRegister`하므로(`MonoRoutine.cs:119-128`)애초에 `isInSceneOnly=true`로 둘 실익이 없다 -
중복 안전장치일 뿐이면서 이런 부작용만 만든다.

**수정**: `EmergencyPanelController.cs`에 `FPSLogger`/`WasdHintController`와 동일한 패턴으로
`Initialize()`를 오버라이드해 `isInSceneOnly = false` 추가.

**확실하지 않음 - 다음에도 안 되면 확인해달라고 할 것**: 이 가설이 100% 확정된 건 아니다
(`ComputerViewController`/`PauseInputController`도 같은 조건인데 그쪽은 멀쩡히 동작 중이라
정확히 같은 매커니즘인지는 불확실). 이번 수정 후에도 여전히 안 눌리면, Unity 콘솔에서
"`UnRegister : Script.Room.EmergencyPanelController`" 로그(`Processor.cs:161`,
`LogHelper.LogTask`)가 찍히는지 확인해달라고 요청할 것 - 찍히면 이 가설이 맞는 것이고
(다른 원인을 찾아야 함을 의미), 안 찍히면 3D 레이캐스트 오클루전 쪽을 다시 조사해야 한다.

**미검증**: 재생 후 계기판 클릭이 실제로 반응하는지(평상시 CLI 피드백 로그부터) 확인 필요.

## 25-5. 텍스트 스크롤 - 25-3의 "이미 구현돼 있음" 판단이 틀렸음, 진짜 원인 발견 (2026-07-20)

**증상**: 사용자 보고 - "텍스트 스크롤 안내려감". 25-3에서 `ScrollRect`/`ContentSizeFitter`
배선이 전부 있는 걸 보고 "이미 정상"이라고 판단했었는데, **배선은 있어도 마우스 휠 이벤트가
그 배선까지 아예 도달하지 못하는 문제가 따로 있었다** - 배선 존재 여부만 확인하고 실제 이벤트
전달 경로까지는 확인 안 한 게 오판의 원인.

**진짜 원인**: `ComputerViewController.RaycastCanvasGraphics()`(직접 구현한 커스텀
레이캐스트, `ComputerViewController.cs:339-361`)는 `GraphicRegistry`에 등록된 그래픽 중
`raycastTarget == true`인 것만 후보로 삼는다. `NotepadPopup`의 `BodyText`
(`ComputerScreenCanvas.prefab` fileID 100000284)는 `m_RaycastTarget: 0`이었고,
`BodyScrollView`(`ScrollRect`가 붙은 오브젝트) 자신도 `Image`가 없어 raycast 후보가 아니다.
그 결과 텍스트 영역에 마우스를 올렸을 때 유일하게 걸리는 raycast 대상은 뒤에 깔린
`Window`의 배경 `Image`(`m_RaycastTarget: 1`)뿐이었다. `ExecuteEvents.ExecuteHierarchy()`는
맞은 오브젝트에서 **위쪽(부모) 방향으로만** 핸들러를 찾는데, `Window`는 `BodyScrollView`의
부모가 아니라 형제 관계(둘 다 `Window`의 자식)라서, `hit`가 `Window`가 되는 순간
`ScrollRect`(`IScrollHandler`)는 그 조상 체인에 없어 스크롤 이벤트가 갈 곳을 잃었다 -
다른 곳(`LibraryPanel`/`ExamControlPanel`/`VisualMemoPanel`)은 행(row)마다 별도
`raycastTarget=true` `Image`를 만들어 그게 `Content`의 자식이라 문제가 없었는데,
`NotepadPopup`은 행 단위 조립이 없이 `BodyText` 하나가 통째로 내용을 담아서 이 구조가
빠져있었다.

**수정**: `BodyText`(fileID 100000284)의 `m_RaycastTarget`을 `0` → `1`로 변경 - 이제
텍스트 영역에 마우스를 올리면 `BodyText` 자신이 `hit`이 되고, 그 부모인 `BodyScrollView`가
`ScrollRect`를 들고 있어 `ExecuteHierarchy`가 정상적으로 찾아낸다. `Graphic.Raycast()`가
내부적으로 조상의 `ICanvasRaycastFilter`(`RectMask2D` 포함)를 이미 확인하므로, 스크롤로
가려진 영역 밖 텍스트가 잘못 클릭되는 부작용도 없다.

**미검증**: 에디터에서 긴 문서를 열어 마우스 휠로 실제로 스크롤되는지 확인 필요.

## 25-7. 계기판 클릭 3차 재확인 실패 + 카메라 전환 중 입력 차단 (2026-07-20)

**증상**: 25-6 수정(`isInSceneOnly = false`) 후에도 사용자 재확인 - "여전히 클릭 안됨.
로그도 안뜸"(CLI 피드백 로그 기준). 반면 "CLI쪽 클릭은 잘 되는데"라고 확인해줘서 -
`ComputerViewController.ProcessScreenPointer()` 경로(2D 화면 클릭) 자체는 멀쩡하고, 오직
`EmergencyPanelController`의 3D 레이캐스트 경로만 문제라는 게 명확해짐. `isInSceneOnly` 이론도
`RegisterComponent()`가 실제로 기존 인스턴스에 inject를 강제 실행한다는 것까지 VContainer
소스(`ComponentRegistrationBuilder.Build()` - `ExistingComponentProvider(instance, injector, ...)`)
로 재확인해봤지만 이것도 이론상 문제없어야 한다는 결론만 나와서, 정적 분석만으로는 원인을
확정 못 함.

**임시 조치 - 진단 로그 추가**: `EmergencyPanelController.UpdateProcess()`를 좌클릭이 있을 때
**게이트를 거치기 전에 무조건** `LogHelper.Log`(Unity 콘솔에 직접 찍힘, CLI 경유 안 함)로
`blockedByComputer`/`blockedByTransition`/`mainCamera` null 여부/`buttonColliders` 개수/
`mutationService` null 여부/레이캐스트 히트 오브젝트 이름을 전부 남기도록 임시 계측을 넣었다.
다음 클릭 시도 때 **Unity 콘솔**(CLI 아님)에 이 로그가 뜨는지, 뜬다면 정확히 어떤 값들이
찍히는지 알려달라고 해야 한다 - 로그 자체가 안 뜨면 `UpdateProcess`가 진짜로 안 불리는 것이고
(등록/DI 문제), 로그는 뜨는데 `hit=none`이거나 엉뚱한 이름이면 레이캐스트가 다른 걸 맞고
있거나 허공을 맞고 있다는 뜻이라 원인이 확정된다. 원인 확정되면 이 진단 블록은 제거한다.

**별개 요청 - 카메라 전환 중 입력 차단**: 사용자 요청 - "카메라 이동 중에는 입력이 안되도록
해줘". `ComputerViewController`에 `IsTransitioning` 공개 프로퍼티 추가, `TransitionTo()`
시작 시 `true`로 켜고 트윈 `OnComplete`에서 `false`로 끄도록 변경(기존엔 `Computer` 시점에
도착할 때만 콜백이 붙어있어서 전환 완료 감지가 그 경우에만 가능했음 - 이제 4개 시점 전환
전부 완료 콜백을 받는다). `UpdateProcess()` 맨 앞에서 `IsTransitioning`이면 그 프레임의
WASD/화면 클릭을 전부 건너뛰고, `EmergencyPanelController`의 게이트에도
`IsTransitioning`을 추가해 계기판 클릭도 전환 중엔 막는다.

**미검증**: 카메라 전환 중 WASD 연타/클릭이 실제로 무시되는지, 계기판 클릭 진단 로그가
Unity 콘솔에 찍히는지(찍힌다면 그 내용) 확인 필요.

## 26. 계기판 클릭 - 진단 로그로 원인 확정(Camera.main 타이밍) + Addressables 핸들 누수 수정 (2026-07-20)

**계기판 클릭 - 드디어 원인 확정**: 사용자가 `logs.md`에 진단 로그를 남겨줌 -
`EmergencyPanel click diag - blockedByComputer=False blockedByTransition=False
mainCamera=False buttonCount=4 mutationService=True hit=none`. **`mainCamera=False`가
결정적 증거** - `Camera.main`이 `Constructor()` 시점엔 아직 null을 반환하고 있었다(씬의
Main Camera는 `MainCamera` 태그가 정상인데도, 그 이른 시점엔 아직 태그 조회가 안 맞았던
것으로 보임). `ComputerViewController`는 같은 Main Camera GameObject에 자기 자신의
`GetComponent<Camera>()`를 쓰기 때문에 이 타이밍 문제를 아예 겪지 않았다 - 그래서 "CLI/화면
클릭은 잘 되는데 계기판만 안 된다"는 사용자 관찰과 정확히 일치했다. 25-4(게이트 오인)와
25-6(`isInSceneOnly`)은 둘 다 틀린 가설이었음이 이걸로 확인됨(`isInSceneOnly` 수정 자체는
해가 안 되니 유지).

**수정**: `EmergencyPanelController.cs` - `Constructor()`에서 한 번만 캐시하던 걸
`UpdateProcess()`에서 `mainCamera == null`이면 매 프레임 다시 시도하도록 변경(자가 복구,
찾고 나면 더 재시도 안 하니 비용 없음). 오인했던 게이트 관련 주석/진단 로그 블록 전부 정리.

**errors.md - Addressables "Attempting to use an invalid operation handle" 원인 발견 및
수정**: 사용자가 "errors는 여전히 해결 안된거 알지?"라고 재확인 - Editor Play 종료
(`Addressables.PlayModeStateChangedCleanup`) 시점에 발생하는 예외였다. 원인:
`SceneUIManager.ClosePeekPanel()`(`Assets/Haare/Scripts/Client/UI/UiManager/SceneUiManager.cs:305-340`,
Haare 프레임워크 공용 코드)이 패널을 닫을 때 `Destroy(panel.panel)`만 부르고 있었는데,
패널은 애초에 `AssetLoader.InstantiatePrefab()`(`Addressables.InstantiateAsync`)로 만들어진
것이라 `Addressables.ReleaseInstance()`로 짝을 맞춰 해제해야 한다. 그냥 `Destroy()`만 부르면
GameObject는 없어져도 Addressables 내부의 핸들 추적은 안 풀려서, Pause/Settings처럼 세션
중 여러 번 열고 닫는 패널마다 핸들이 하나씩 샜다 - Editor Play를 멈출 때 Addressables가
아직 추적 중인 핸들을 전부 정리하려다, 이미 우리가 `Destroy()`로 먼저 없애버린 GameObject를
가리키는 핸들을 만나 "invalid operation handle" 예외를 던진 것.
- **수정**: `Destroy(panel.panel)` → `Addressables.ReleaseInstance(panel.panel)`로 교체
  (반환값이 `false`일 때만, 즉 애초에 Addressables 추적 대상이 아니었을 때만 안전하게
  `Destroy()`로 폴백). `ReleaseInstance`가 내부적으로 파괴까지 처리하므로 중복 파괴 없음.
- **영향 범위**: `NotepadPopup`은 plain `Instantiate()`(Addressables 아님)라 원래도 문제
  없었음, `LibraryPanel`의 행(row) `Destroy`도 수동 생성 오브젝트라 무관 - 프로젝트 전체에서
  이 문제가 있던 곳은 `ClosePeekPanel()` 한 곳뿐이었음(`grep`으로 다른 `Destroy()` 호출부
  전수 확인).

**미검증**: 계기판 클릭이 이제 실제로 반응하는지(평상시 CLI 피드백 로그 → 변이 상태에서
실제 단계 진행까지), Editor Play를 멈출 때 그 Addressables 예외가 더 이상 안 뜨는지(Pause/
Settings를 몇 번 여닫은 뒤 정지해서 확인하는 게 재현 가능성이 높음) 확인 필요.

## 26-1. 계기판 클릭 확인됨 / Addressables 예외 재현 - 진짜 범위는 훨씬 넓었음 (2026-07-20)

**계기판 클릭**: 사용자 확인 - "클릭은 잘 되었어". 26장 수정으로 해결 완료.

**Addressables 예외 - 여전히 재현됨**: 사용자 재확인 - "에러는 여전히 뜸". 26장에서
`ClosePeekPanel()` 한 곳만 고친 게 **범위가 너무 좁았다** - 다시 보니 이 프로젝트의 패널
대부분(`BioSearchUIPresenter`가 부팅 때 여는 7개 정적 패널, `TitlePresenter`의
`TitlePanel`)은 `IsStack:false`로 로드되어 **애초에 `ClosePeekPanel()`을 절대 거치지
않는다** - ssh/Title 씬이 전환될 때 Unity의 씬 언로드가 그 GameObject들을 통째로 파괴하는
것으로 끝나버리고, 그 어떤 코드도 `Addressables.ReleaseInstance()`를 부르지 않은 채
지나간다. 즉 26장에서 고친 건 Pause/Settings처럼 명시적으로 닫히는 소수 패널뿐이었고,
훨씬 큰 비중을 차지하는 "씬 언로드로 그냥 없어지는" 패널들은 여전히 핸들이 새고 있었다.

**수정 - 창구를 하나로 통일**: 패널마다 닫히는 경로(명시적 `ClosePeekPanel`/암묵적 씬
언로드)가 제각각이라 호출부마다 일일이 `ReleaseInstance`를 넣는 건 끝이 없다 - 대신
`Haare.Client.Routine.MonoRoutine.OnDestroy()`(모든 패널/`MonoRoutine` 파생 클래스가
공통으로 거치는 단일 지점)에 `Addressables.ReleaseInstance(gameObject)`를 추가했다.
이 GameObject가 애초에 Addressables로 만들어진 게 아니면(`AddressablesImpl.
ReleaseInstance` 내부의 `m_resultToHandle` 사전 조회가 실패) 조용히 `false`만 반환하고
아무 부작용이 없으므로, 씬에 미리 배치된 컴포넌트(`ComputerViewController`,
`EmergencyPanelController` 등)에도 안전하게 걸 수 있다. 예외 발생 가능성에 대비해
`try/catch`로 감쌌다(파괴되는 중인 오브젝트 정리라 실패해도 게임에 영향 없음).
- **26장에서 `ClosePeekPanel()`에 직접 넣었던 `Addressables.ReleaseInstance` 호출은
  되돌렸다** - 그대로 두면 `ReleaseInstance`(명시적) → 내부 `Destroy` → 그 오브젝트의
  `OnDestroy`(새로 추가한 공용 처리) → `ReleaseInstance`를 또 호출하는 **이중 해제
  경로**가 생겨서, 같은 핸들을 두 번 `Release()`하다가 오히려 "invalid operation handle"을
  더 자주 재현시킬 위험이 있었다. 이제 `ClosePeekPanel()`은 다시 평범한 `Destroy()`만
  부르고, 실제 Addressables 해제는 `MonoRoutine.OnDestroy()` 한 곳에서만 일어난다(명시적
  닫기든 씬 언로드든 경로 상관없이 전부 이 한 지점을 반드시 거침).

**미검증**: 씬 전환(ssh ↔ Title)을 몇 번 거친 뒤 Editor Play를 멈춰서 그 Addressables
예외가 이번엔 진짜로 안 뜨는지 확인 필요 - 안 뜨면 완전히 해결된 것이고, 그래도 뜨면
`MonoRoutine`이 아닌 다른 경로(예: `NotepadPopup`처럼 `MonoRoutine`을 안 쓰는 오브젝트,
또는 씬 자체가 Addressable로 로드되는 경우)로 범위를 더 넓혀야 한다.
