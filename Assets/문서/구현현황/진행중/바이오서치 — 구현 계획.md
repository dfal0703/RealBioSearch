# 바이오서치 — 구현 계획

작성일: 2026-07-14
근거 문서: `기획문서/바이오서치 — 핵심 기획.md`, `바이오서치 — 전체 기획 정리.md`,
`바이오서치 — 개발 구현 지시서.md` (세 문서 모두 검토 완료)
프레임워크 세팅 현황: `프레임워크/Haare_프레임워크_정리.txt` (완료 상태)

이 문서는 개발 구현 지시서의 1~8단계 설계를 Haare 프레임워크(Panel/Routine/DataManager/VContainer)
구조에 매핑한 **기술 구현 계획**이다. 기획 문서 자체는 수정하지 않았다.

---

## 0. 기획 문서 검토 중 확인한 사항 (사용자 답변 반영)

> **[2026-07-14 갱신] 방향 전환**: 아래 "실제 3D 씬은 만들지 않는다"는 초기 판단은 이후 실제
> 플레이가 진행되며 뒤집혔다. 지금은 방(Table+컴퓨터 프리팹)이 실제 3D 씬으로 존재하고, WASD로
> "프레디의 피자가게 4"식 4포인트 카메라 전환(W=컴퓨터 정면, A=좌측, D=우측, S=중앙 복귀)을 하는
> 구조로 구현했다(`Assets/Script/Room/ComputerViewController.cs`). 컴퓨터 화면은 컴퓨터 프리팹의
> `screenON` 메쉬에 전용 카메라(`ComputerScreenCamera`)가 렌더텍스처로 캔버스를 그려 넣는 방식으로
> 방 시점에서도 보이고, W로 컴퓨터를 바라보면 같은 캔버스가 Screen Space Overlay로 전환되며 실제
> 마우스 상호작용이 가능해진다. 아래 문단의 "2D 연출로 대체" 서술은 더 이상 유효하지 않다 —
> `SubjectMonitorPanel` 등 UI 패널들은 이제 이 컴퓨터 화면 캔버스 안에 올라가는 컨텐츠로 위치가
> 바뀐다. 자세한 구현 내역은 4장 "구현 완료 내역" 참고.

기획 문서에는 "3D 검사실/좌측 상단 3D 검사실 카메라"라는 표현이 여러 번 나오지만, ~~확인 결과 실제
3D 씬은 만들지 않는다~~ (위 갱신 참고) — 카메라로 촬영하는 것처럼 보이는 2D 스프라이트 교체 연출(모니터
프레임, 스캔라인/노이즈 오버레이, 타임스탬프 같은 "감시 카메라 처럼 보이게 하는" 연출)로 대체한다는
게 초기 판단이었다. 기획 문서의 "3D 공간/검사실 관찰/비상 버튼 조작" 항목을 이 2D 연출 패널 하나의
기능으로 재해석하려 했으나, 실제로는 진짜 3D 룸 + 카메라 전환 쪽으로 구현이 진행되었다. 이 문서
전체에서 "SubjectMonitorPanel"은 이제 기획서의 "3D 검사실"이 아니라 컴퓨터 화면 캔버스 안 패널에
대응한다.

프로토타입(1~4단계) 아트는 플레이스홀더 스프라이트로 시작한다(보유 에셋이 생기면 교체).

사례 데이터(대화 로그/수집 자료/최종판정 초안 등 진행 상태)는 **Haare DataManager +
[DataModelAttribute] 경로로 처음부터 로컬 저장**한다 — 재실행해도 진행 중이던 사례가 이어져야
한다.

---

## 1. 프로젝트 코드 폴더 구조 (신설)

Haare 프레임워크 자체(`Assets/Haare/`)와 분리해서, 이 프로젝트 전용 코드는 `Assets/Script/` 아래
둔다 (`문서 가이드.md` 개발 철학 4번: "모든 파일은 각자 제 자리 폴더에").

```
Assets/Script/
  DI/                 BioSearchCompositionRoot : CoreLifetimeScope
  Presenter/          BioSearchUIPresenter : UIPresenter (부팅 시퀀스)
  Data/
    CaseFile.cs                 [DataModelAttribute] 사례 진행 상태 모델
    CaseFileDefinition.cs       사례 "정답지"(정적 저작 데이터, JSON 직접 로드 — DataManager 미사용)
  Service/
    CaseSessionService.cs       사례 접수 → 진행 → 종료 상태 머신 (NativeRoutine)
    ExamService.cs              검사 부위+방식 실행 → 결과 자료 생성 (NativeRoutine)
    DialogueService.cs          대화 진행/로그 기록 (NativeRoutine)
    CaseTimeService.cs          현실시간:게임시간 비율로 남은 시간 관리 (NativeRoutine)
    HealthService.cs            숨겨진 체력/기생체 자극도 추적 (NativeRoutine)
    MutationService.cs          자극도 임계치 초과 시 변이/비상 트리거 (NativeRoutine)
  UI/Panels/
    SubjectMonitorPanel.cs       기획서의 "3D 검사실" 역할, 2D 스프라이트 연출
    VisualMemoPanel.cs
    HealthStatusPanel.cs
    FinalReportPanel.cs
    ExamControlPanel.cs
    LibraryPanel.cs
    CLIPanel.cs
```

새 패널은 전부 `Assets/Haare/Demo/Script/UI/DebugPanel.cs`를 복제해서 시작한다
(`[PanelAttribute]` + `ICustomPanel`, Haare 세팅 문서 9-1장 참고).

프로젝트 전용 서비스가 생기는 시점이므로, `BioSearchCompositionRoot : CoreLifetimeScope`를 만들고
`Configure()` 맨 앞에서 `base.Configure(builder)` 호출 → 위 6개 서비스를 추가 등록한다. 이 스크립트를
붙인 프리팹을 만들어 `Assets/Haare/VContainerSettings.asset`의 `RootLifetimeScope`를
`CoreLifeTimeScope.prefab`에서 이 새 프리팹으로 교체한다(Haare 세팅 문서 12-3장에 미리 적어둔
절차 그대로).

---

## 2. 단계별 구현 계획

### Stage 0 — 프레임워크 세팅 (완료)
Haare 소스/패키지/DOTween/Addressables 설정 완료. `Haare_프레임워크_정리.txt` 참고.

### Stage 1 — 기본 플레이 공간 구축 (구현 완료, 에디터 검증 대기 — 2026-07-15, 4장 참고)
**목표**: 화면 레이아웃 확정, 패널 틀만 존재하는 상태로 동시 표시.

1. `BioSearchCompositionRoot` 생성 + Root LifetimeScope 교체(위 1장 절차).
2. `BioSearchUIPresenter : UIPresenter` 생성, 부팅 시 아래 7개 패널을 `IsStack:false`로 프리로드
   (Haare 세팅 문서 6장의 `OpenPanelWithFade` 버그 회피 원칙 그대로 적용).
3. 화면 레이아웃(문서 "화면 구성" 절 기준, 앵커 배치만 — 실제 좌표는 러프하게):
   - 좌상단: `SubjectMonitorPanel` (플레이스홀더 스프라이트 1장 + 모니터 프레임 테두리만)
   - 좌중: `VisualMemoPanel` (빈 보드)
   - 좌하: `HealthStatusPanel` + `FinalReportPanel` (텍스트 placeholder)
   - 중앙: `ExamControlPanel` (드롭다운 2개 + 버튼, 아직 로직 없음)
   - 우상: `LibraryPanel` (빈 리스트)
   - 우하: `CLIPanel` (텍스트 로그 + 입력창, 아직 명령 처리 없음)
4. **완료 기준**: 앱 실행 시 7개 패널이 동시에 뜨고, `SubjectMonitorPanel`에 더미 이미지가 보인다.
   실제 검사 로직·대화·판정은 아직 없어도 된다.

### Stage 2 — 검사 사례 진행 구조
1. `CaseFile`(`[DataModelAttribute]`, 로컬 JSON 저장) 정의: 사례 ID, 진단 보고서 필드(이름/성별/
   나이/신분/직업/경위/기존질환/신고증상/최근행동이상), 진행 상태(대기/진행중/종료), 첨부 파일 목록,
   대화 로그, 최종 판정 초안.
2. `CaseFileDefinition`(정적 저작 데이터, JSON 직접 로드 — DataManager 미사용, Haare 세팅 문서
   7장의 "에디터에서 미리 굽는 데이터" 구분 그대로 적용): 튜토리얼 사례 1건의 "정답"(감염 여부,
   감염 부위, 대화 응답, 검사 반응 등 하드코딩된 컨텐츠).
3. `CaseSessionService`: 앱 시작 시 저장된 `CaseFile`이 있으면 그 상태로 재개, 없으면 새 사례를
   `CaseFileDefinition`에서 생성. `CLIPanel`에 "검사 요청 수신" 로그 출력.
4. `FinalReportPanel`에서 제출 버튼 → 사례 종료 → 저장된 `CaseFile` 삭제/보관 처리.
5. **완료 기준**: 사례 하나가 접수→(더미 진행)→보고서 제출까지 끊김없이 진행되고, 진행 중 앱을
   재실행해도 같은 사례가 이어진다.

### Stage 3 — 조사와 자료 관리
1. `DialogueService`: 간단한 선택형 대화 트리(초기엔 정해진 질문/답변 목록), 대화 내용을
   `CaseFile.대화로그`에 자동 추가.
2. `LibraryPanel`: `CaseFile`의 파일 목록을 문서/대화/오디오/이미지/수치 카테고리로 표시, 클릭 시
   내용 열람.
3. `VisualMemoPanel`: 이미지 자료를 클릭으로 보드에 배치(드래그는 이후 단계로 미룸).
4. **완료 기준**: 보고서+대화 정보로 의심 부위를 하나 이상 추론할 수 있고, 자료가 라이브러리에
   일관되게 누적된다.

### Stage 4 — 핵심 검사 루프
1. `ExamControlPanel`: 검사 부위 드롭다운(초기 3~4종) + 검사 방식 드롭다운(문진/관찰/음향/전기 중
   일부만 우선 구현) + 실행 버튼.
2. `ExamService`: 부위+방식 조합 → `CaseFileDefinition`에서 미리 정의된 결과(텍스트/수치, 이미지는
   플레이스홀더 스프라이트)를 찾아 `CaseFile`에 결과 파일로 추가.
3. `SubjectMonitorPanel`: 검사 실행 시 스프라이트 교체 + 짧은 노이즈/글리치 연출(DOTween).
4. **완료 기준**: 부위/방식 조합에 따라 다른 결과 자료가 생성되고 라이브러리에 쌓인다.

### Stage 5 — 검사 위험과 상태 변화
1. `CaseTimeService`: 실시간:게임시간 비율(초기값은 임의 설정 후 플레이 테스트로 조정) 기반 남은
   시간 관리.
2. `HealthService`: 검사 방식별 위험도에 따라 숨겨진 체력 감소, `HealthStatusPanel`에 5단계(안정/
   경미한 이상/불안정/위험/위급)로만 노출.
3. **완료 기준**: 위험한 검사를 반복하면 실제로 남은 시간/건강 상태가 나빠지는 게 체감된다.

### Stage 6 — 기생체 변이와 비상 상황
1. `MutationService`: 기생체 자극도 누적 → 임계치 초과 시 변이 트리거.
2. `SubjectMonitorPanel`: 변이 시 연출 급변(글리치 심화), 비상 버튼 UI 등장(2D 버튼, 3D 공간
   아님).
3. 비상 대응 처리 → 검사 강제 종료 + 사고 기록 파일 생성.
4. **완료 기준**: 특정 조건에서 변이가 발생하고, 비상 버튼으로 상황을 종료할 수 있다.

### Stage 7 — 최종 판정 시스템
1. `FinalReportPanel`: 감염 여부 + 감염 부위 선택 UI.
2. 제출 시 `CaseFileDefinition`의 정답과 대조해 결과(정답/오답, 검사체 생존 상태) 판정.
3. **완료 기준**: 제출 정확도와 검사체 상태에 따른 결과를 확인할 수 있다.

### Stage 8 — 튜토리얼 챕터 통합
1. Stage 2~7까지 만든 걸 사례 1건으로 이어 붙이고, 각 단계 사이에 짧은 CLI 안내 텍스트 삽입.
2. **완료 기준**: 외부 설명 없이 플레이어가 스스로 다음 사례부터 기본 업무를 진행할 수 있다
   (기획서 10장 완료 기준과 동일).

---

## 3. 다음 액션

Stage 1부터 순서대로 진행한다. 각 Stage는 독립적으로 실행 가능한 상태를 유지한 뒤 다음 Stage로
넘어간다(개발 구현 지시서 2장의 "단계별 플레이 가능 상태 유지" 원칙).

---

## 4. 구현 완료 내역

### 2026-07-14 — 방(3D) ↔ 컴퓨터 화면(2D) 카메라 전환 뼈대

**배경**: 사용자가 방 시점(3D)에서 시작해 W로 컴퓨터를 바라보며 UI와 상호작용하고, S로 방으로
복귀하는 구조를 지시. WASD 4개 키 = "프레디의 피자가게 4"식 4포인트 카메라 전환(W=컴퓨터,
A=좌측, D=우측, S=중앙/방). 위 0장의 "실제 3D 씬은 안 만든다" 판단을 뒤집는 방향 전환.

**구현 내역**:
- `Assets/Script/Room/ComputerViewController.cs` (신규) — Haare `MonoRoutine` 상속. 인스펙터에
  4개 카메라 포인트 Transform + 컴퓨터 캔버스/레이캐스터/스크린카메라 참조가 필요해 `NativeRoutine`
  대신 `MonoRoutine`을 선택(Haare 프레임워크 원칙 3장 그대로 적용). `UpdateProcess()`에서 새
  Input System(`Keyboard.current`)으로 WASD를 감지해 DOTween으로 Main Camera를 4개 포인트 중
  하나로 이동시키고, 컴퓨터 포인트 진입/이탈 시 캔버스의 `RenderMode`를 `ScreenSpaceCamera`
  (렌더텍스처 전용, 상호작용 불가) ↔ `ScreenSpaceOverlay`(전체화면, 상호작용 가능)로 토글한다.
  씬에 새 GameObject를 만들지 않고 Main Camera에 컴포넌트로 직접 부착해 오브젝트 수를 최소화했다.
- (2026-07-14 이후 런타임 생성 방식으로 교체됨 — 아래 "RenderGraph 렌더텍스처 에러" 항목 참고)
  `ComputerScreenCamera`(UI 레이어만 렌더)가 렌더텍스처로 캔버스를 그리고, 컴퓨터 프리팹
  (`CRTMonitor`)의 `screenON` 메쉬 렌더러 인스턴스에 그 텍스처를 코드로 입혀서 — 방 시점에서도
  모니터에 실시간 UI가 비친다.
- `ssh.unity` — `RoomCameraPoints`(CenterPoint/ComputerPoint/LeftPoint/RightPoint 4개 빈
  Transform, 스크립트 없음) + `ComputerScreenCamera` + `ComputerScreenCanvas`(플레이스홀더 배경+
  텍스트만 있는 상태, 실제 BioSearch 패널은 아직 없음) 추가. 4개 포인트 좌표는 방에 벽/바닥
  지오메트리가 아직 없어 임시값(현재 Main Camera 위치를 Center로, 컴퓨터 근접샷을 Computer로,
  좌우 45도 회전을 Left/Right로)으로 배치 — 방 오브젝트가 채워지면 에디터에서 눈으로 보며
  재조정 필요.
- `Assets/Haare/Scripts/Client/Core/HaareClient.cs` (버그 수정) — 프로젝트의 Active Input
  Handling이 New Input System 전용(`activeInputHandler: 1`)인데 EventSystem에 레거시
  `StandaloneInputModule`을 붙이고 있던 것을 발견 — 이 상태로는 WASD 감지는 물론 기존 UI 클릭도
  전부 동작하지 않았을 것. `InputSystemUIInputModule`로 교체.

**남은 일**: 방에 벽/바닥 등 실제 지오메트리가 생기면 4개 카메라 포인트 위치/회전을 에디터에서
재조정. `ComputerScreenCanvas`의 플레이스홀더 UI를 1장 Stage 1의 실제 7개 패널로 교체(패널을 이
캔버스 하위에 배치하는 구조로).

### 2026-07-14 — 프레임워크 버그 수정 + BioSearchCompositionRoot 도입

**배경**: 위 카메라 전환 뼈대를 플레이 테스트하던 중 (1) Play를 누를 때마다 `errors.md`에 렌더
에러가 매 프레임 반복 축적, (2) Hierarchy에 `Processor`가 Play할 때마다 하나씩 늘어나는 문제 발견.
사용자가 이전 프로젝트 `GrimArchive_Prototype`(같은 Haare 프레임워크 벤더링 사용)의 프레임워크
코드를 참고해 문제를 대조해보라고 지시.

**원인 및 수정**:
- **Processor 중복**: 원인은 코드가 아니라 씬 저장 습관이었다 — Play 모드로 들어간 상태에서 씬을
  저장하면 그 순간 런타임에 살아있던 `Processor`가 씬 에셋에 그대로 박제된다. 두 번 그런 일이
  있어서 `ssh.unity`에 `Processor` GameObject가 2개 저장돼 있었음 — 둘 다 제거.
  근본 수정: 사용자 지시로 "Processor는 씬에 실제로 존재하게" 방향 전환 — 즉흥 재생성이 아니라
  `ssh.unity`에 `Processor` GameObject를 하나 정식으로 배치했다. 이를 지원하려고
  `Assets/Haare/Scripts/Client/Core/Singleton/SingletonMono.cs`의 `Instance` getter를 수정:
  기존엔 무조건 `Create()`(새 GameObject 생성)부터 불렀는데, 씬에 이미 있으면 `FindObjectOfType`로
  먼저 찾아 재사용하고 없을 때만 새로 만들도록 변경. `Processor.cs`의 `Constructor()`에
  `DontDestroyOnLoad(gameObject)`도 추가(씬 배치 경로는 `Create()`를 안 타서 이게 없으면 씬 전환
  시 씹힘).
- **GrimArchive_Prototype 대조로 찾은 프레임워크 버그**(둘 다 같은 시점에 벤더링된 Haare 원본에서
  갈라졌고, GrimArchive 쪽에서 이미 고쳐져 있던 것들 — 그대로 이식):
  - `HaareClient.cs` — `EventSystem`/`AudioListener`를 씬에 이미 있어도 무조건 새로 만들고 있었음
    (Main Camera에 이미 `AudioListener`가 있는데 하나 더 만들어서 "2 audio listeners" 경고 발생
    중이었을 것). `FindObjectOfType(...) == null`일 때만 생성하도록 수정. `Application.isPlaying`
    가드도 추가(도메인 리로드 시 플레이 모드가 아닌데도 `RuntimeInitializeOnLoadMethod`가 불려
    `DontDestroyOnLoad` 예외가 날 수 있음).
  - `CoreLifetimeScope.cs` — `_coreUIManagerPrefab`을 `private → protected`(프로젝트 전용
    CompositionRoot가 상속해서 접근할 수 있어야 함). `CoreUIManager` 등록에
    `.As<SceneUIManager>()` 추가 — 이게 없으면 나중에 `UIPresenter` 상속 클래스를 실제로 만드는
    순간 `SceneUIManager` DI 해석이 깨진다(아직 하나도 안 만들어서 지금은 안 터짐, 미리 막음).
  - `CustomSlider.cs` — 슬라이더 참조를 `Initialize()`(비동기)에서만 채우고 매번
    `Setup(0,1,0)`으로 강제 리셋해서, 외부에서 지정한 min/max/value를 나중에 덮어쓰는 버그.
    `Constructor()`(동기, Awake 시점)에서 먼저 채우도록 수정.
  - `AssetLoader.cs` — `SaveJson()`이 하위 폴더가 없는 경로에 저장하려 하면
    `DirectoryNotFoundException`이 조용히 삼켜져 저장 실패를 못 알아채는 버그. 저장 전 디렉터리
    자동 생성 추가 — `CaseFile` 로컬 저장(Stage 2)에서 실제로 마주쳤을 문제라 미리 막음.
  - (`MonoRoutine.cs`/`NativeRoutine.cs`/`INativeRoutine.cs`/`CustomButton.cs`는 `R3.Unit` 정규화
    표기 차이뿐이라 기능 차이 없음, 이식 안 함.)
- **RenderGraph 렌더텍스처 에러**: `Processor` 수정 이후에도 "Invalid imported texture" 예외가
  매 프레임 반복. 손으로 작성한 `.renderTexture` 에셋의 Depth Stencil Format 값(처음엔 None, 다음엔
  다른 프로젝트 샘플에서 가져온 90)이 둘 다 이 카메라 용도엔 안 맞았던 것으로 추정 — 에셋 포맷
  enum을 손으로 추측하는 방식 자체가 반복적으로 문제를 일으켜서, `ComputerViewController.
  Constructor()`에서 `new RenderTexture(...)`로 **런타임에 코드 생성**하도록 전환(포맷은 Unity가
  알아서 채움). 정적 `.renderTexture`/`.mat` 에셋은 삭제, `screenON` 머티리얼 텍스처도 코드에서
  직접 `SetTexture`.
- **`Assets/Script/DI/BioSearchCompositionRoot.cs`(신규)** — 이 프로젝트 최초의 프로젝트 전용
  VContainer 등록 지점. `CoreLifetimeScope`를 상속하지 않는 **독립 자식 LifetimeScope**로 설계 —
  부모를 명시하지 않으면 VContainer가 자동으로 루트 스코프(`CoreLifeTimeScope.prefab`, 아직 그대로
  유지)를 부모로 잡아준다(`LifetimeScope.GetRuntimeParent`). `base.Configure()`를 호출하는
  `CoreLifetimeScope` 상속 방식은 GrimArchive_Prototype에서 실제로 쓰고 있었지만, 씬에 배치된
  자식 스코프가 `DataManager`/`SceneService`/`CoreUIManager`를 다시 등록하면 루트 프리팹의 등록과
  별개의 인스턴스가 생길 위험이 있어(부모/자식 두 컨테이너에 같은 타입이 따로 등록됨) 채택하지
  않음 — 대신 새로 추가하는 것만 등록하는 쪽을 기본값으로 삼는다. 지금은 `ComputerViewController`
  하나만 `RegisterComponentInHierarchy<T>()`로 등록(씬 전체에서 찾아 DI 그래프에 편입, 카메라
  포인트/캔버스 같은 공간 참조는 여전히 Inspector 배선 필요 — DI가 그것까지 대신하진 않음).
  `ssh.unity`에 `BioSearchCompositionRoot` GameObject로 배치. 앞으로 `CaseSessionService` 등
  Stage 2+ 서비스도 여기 등록한다.

### 2026-07-15 — ComputerScreenCanvas 프리팹화 + 실행 시점 생성 전환

**배경**: `ssh.unity`에 직접 박혀 있던 `ComputerScreenCanvas`(플레이스홀더 배경+텍스트)를
재사용 가능한 프리팹으로 분리해 달라는 지시. 1차로는 씬에 `PrefabInstance`로 배치하는 방식으로
작업했으나, 이어서 "씬에 캔버스를 아예 두지 말고 실행 시 프리팹에서 생성하는 방식으로" 방향 전환
지시가 들어와 최종적으로는 런타임 인스턴스화 방식으로 구현했다.

**구현 내역**:
- `Assets/Prefabs/UI/ComputerScreenCanvas.prefab` (신규) — 기존 씬의 Canvas(월드/오버레이 겸용,
  `CanvasScaler`+`GraphicRaycaster`) + `Background` + `Label`(TMP, "BIOSEARCH TERMINAL / READY"
  플레이스홀더 문구) 계층을 그대로 이전. 폴더 위치는 `GrimArchive_Prototype`의
  `Assets/Prefabs/UI/...` 컨벤션을 따름.
- `ssh.unity` — `ComputerScreenCanvas` 관련 GameObject/컴포넌트를 전부 제거. 씬에는 이제 UI가
  전혀 존재하지 않고, `RoomCameraPoints`/`ComputerScreenCamera`/`Table`(+`CRTMonitor` 프리팹
  인스턴스)만 남는다.
- `Assets/Script/Room/ComputerViewController.cs` — `[SerializeField] private Canvas
  computerCanvas` / `GraphicRaycaster computerRaycaster` 두 필드를 제거하고
  `[SerializeField] private GameObject computerCanvasPrefab`로 교체. `Constructor()` 맨 앞에서
  `Instantiate(computerCanvasPrefab)` 후 `GetComponent<Canvas>()`/`GetComponent<GraphicRaycaster>()`로
  런타임 참조를 채운 뒤, 기존 렌더텍스처/화면 메쉬/카메라 전환 로직은 그대로 이어간다.
- 캔버스가 프리팹 애셋 상태라 씬 오브젝트(`ComputerScreenCamera`)를 직접 참조할 수 없어
  `Canvas.worldCamera`는 프리팹에 미리 넣지 않았다 — 원래도 `SetComputerInteractive(false)`가
  `Constructor()` 안에서 항상 한 번 호출되며 `worldCamera`를 채워주므로 동작상 차이 없음.

**Haare 프레임워크 관련 판단(문서 가이드 개발 철학 1번 — "모든 코드는 Haare 프레임워크에 귀속")**:
- `GrimArchive_Prototype`의 `SceneUIManager`/`ICustomPanel`/`PanelAttribute` 패널 시스템(위 1장
  "새 패널은 전부 DebugPanel.cs를 복제" 규칙과 동일 계열)은 Addressable 경로로 로드해 스택으로
  열고 닫는 전체화면 패널 전용이라, 상시 존재하며 RenderMode만 토글되는 이 캔버스에는 그대로
  맞지 않아 적용하지 않았다. 다만 Stage 1에서 만들 7개 실제 BioSearch 패널(`SubjectMonitorPanel`
  등)은 이 문서 1장 계획대로 `[PanelAttribute]`+`ICustomPanel`로 만들어 **이 캔버스 하위에
  얹는 컨텐츠**로 배치할 예정 — 캔버스 자체는 컨테이너, 그 안의 개별 화면들이 Haare Panel이 되는
  구조로 정리한다.
- `BioSearchCompositionRoot`의 DI 등록 대상(`ComputerViewController`)이나 `Processor` 배치 방식은
  건드리지 않음.

**남은 일**: Stage 1 진행 시 `ComputerScreenCanvas.prefab` 안의 `Background`/`Label`(플레이스홀더)을
실제 7개 패널이 얹히는 루트 컨테이너로 교체.

### 2026-07-15 — 흩어진 에셋 경로 정리

**배경**: `문서 가이드.md` 개발 철학 4번("모든 파일은 각자 제 자리 폴더에, 중구난방 금지") 기준으로
`Assets/` 최상위를 점검한 결과 `BioSearch_CRTPost.mat`, `CRTPost.shader` 두 파일이 전용 폴더 없이
`Assets/` 루트에 그대로 놓여 있는 걸 발견.

**구현 내역**:
- `git mv`로 `Assets/CRTPost.shader` → `Assets/Shaders/CRTPost.shader`,
  `Assets/BioSearch_CRTPost.mat` → `Assets/Materials/BioSearch_CRTPost.mat` 이동(각 `.meta` 동반
  이동, GUID 보존이라 참조 깨짐 없음 — 이동 전 두 GUID를 프로젝트 전체에서 검색했으나 아직 씬/
  프리팹/Renderer Feature 어디에도 연결되지 않은 상태였음, 즉 아직 미사용 에셋).
- 폴더명은 `GrimArchive_Prototype`의 `Assets/VFX/Shader`, `Assets/VFX/Material` 컨벤션을 참고하되,
  이 프로젝트엔 아직 VFX 폴더가 없고 CRT 포스트 효과는 파티클 VFX가 아니라 화면 포스트프로세싱이라
  `Assets/Shaders/`, `Assets/Materials/`로 새로 만듦(Unity 기본 관례이기도 함).
- `Assets/` 루트에 남은 `InputSystem_Actions.inputactions`/`NuGet.config`/`packages.config`/
  `Readme.asset`은 전부 툴체인/Unity 기본 템플릿 관례상 루트에 있어야 하는 파일이라 그대로 둠.
  `Assets/Script`(DI/Room), `Assets/Prefabs/UI`, `Assets/문서` 등 프로젝트 전용 폴더들은 이미
  정리된 상태였음.

**남은 일**: `CRTPost.shader`/`BioSearch_CRTPost.mat`을 실제로 어디에 적용할지(Renderer Feature
전역 포스트프로세싱인지, `ComputerScreenCamera` 전용인지 등)는 아직 미정 — 사용 시점에 확인 필요.

### 2026-07-15 — Stage 1: 기본 플레이 공간 구축 (7패널 부팅 시퀀스)

**배경**: 사용자 지시로 1장 계획의 Stage 1 나머지 항목(2~4번: `BioSearchUIPresenter` + 7개 패널
프리로드 + 화면 레이아웃)을 이어서 구현. 기획문서 "화면 구성"(`핵심 기획.md`/`전체 기획 정리.md`
11장)을 재검토해 좌상단/좌중/좌하상부/좌최하단/중앙/우상단/우하단 6영역 7패널 배치를 확정했다.

**설계 충돌과 해결(사용자 확인 받음)**: Haare의 Panel 시스템은 `[Scene]UIManager : SceneUIManager`가
`RegisterComponentInHierarchy`로 찾아지려면 VContainer가 `Configure()`를 실행하는 시점에 이미 씬에
존재해야 하는데, 지난 턴에서 `ComputerScreenCanvas`를 `ComputerViewController.Constructor()`가
실행 시점에 `Instantiate`하는 방식으로 바꿔둬서 시점이 어긋났다(둘 다 서로 다른 GameObject의
`Awake()` 안에서 실행되고 Unity가 그 순서를 보장하지 않음). 사용자에게 확인 후
**`BioSearchCompositionRoot`가 캔버스를 직접 생성**하는 쪽으로 정리했다 —
`CoreLifetimeScope`가 `CoreUIManager` 프리팹을 `RegisterComponentInNewPrefab`으로 등록하는 것과
동일한 패턴이며, `VContainer.Unity.LifetimeScope`에 `[DefaultExecutionOrder(-5000)]`가 붙어 있어
(`Library/PackageCache/jp.hadashikick.vcontainer.../Runtime/Unity/LifetimeScope.cs` 확인)
`Configure()`가 일반 `MonoBehaviour.Awake()`보다 항상 먼저 끝난다는 사실로 순서를 보장한다.

**구현 내역**:
- `Assets/Script/UI/BioSearchUIManager.cs`(신규) — `SceneUIManager` 최소 상속(`CoreUIManager`와
  동일 패턴). `ComputerScreenCanvas.prefab` 루트에 컴포넌트로 부착.
- `Assets/Prefabs/UI/ComputerScreenCanvas.prefab` 수정 — 플레이스홀더 `Label`(BIOSEARCH TERMINAL/
  READY) 제거. 루트에 `BioSearchUIManager` 컴포넌트 추가, `safePannelRect`가 가리킬 빈 `SafePanel`
  RectTransform(전체 스트레치) 자식 추가(`Haare/Demo/.../CoreCanvas.prefab`의 `safePanel` 자식과
  동일 컨벤션). 루트 RectTransform의 `m_LocalScale`이 `{0,0,0}`으로 돼 있던 걸 `{1,1,1}`로 정정
  (Screen Space Canvas라 렌더링엔 영향 없었지만 이제 실제 콘텐츠가 자식으로 붙으므로 정리).
- `Assets/Script/UI/Panels/`(신규 7개) — `SubjectMonitorPanel`/`VisualMemoPanel`/`HealthStatusPanel`/
  `FinalReportPanel`/`ExamControlPanel`/`LibraryPanel`/`CLIPanel`. 전부 `MonoRoutine, ICustomPanel` +
  `[PanelAttribute("Prefabs/UI/Panels/...")]`, `OpenPanel()`만 오버라이드하는 최소 구현 —
  Stage 1 완료 기준이 "패널 틀만 존재"라 로직은 아직 없음. `Haare/Demo/Script/UI/DebugPanel.cs`
  복제 관행을 따르되, DebugPanel이 구버전 `ICustomPanel`(더는 없는 `IDataInstance` 시그니처)을 쓰고
  있어서 그대로 베끼지 않고 현재 인터페이스(`Haare.Client.UI.ICustomPanel`, C# 8 디폴트 구현) 기준으로
  새로 작성했다.
- `Assets/Prefabs/UI/Panels/`(신규 7개 프리팹) — 화면 구성 6영역 앵커 그리드(좌 1/3·중앙 1/3·우 1/3,
  좌측은 상 45%/중 25%/하 30%(하는 다시 상하 절반), 우측은 상하 50%씩)에 맞춰 배치. 전부 배경
  `Image` + `TextMeshProUGUI` 플레이스홀더 조합(Haare의 `CustomButton`/`CustomImage`는 라이트/호버
  스프라이트나 클릭 이벤트가 필요할 때 쓰는 것이라, 아직 상호작용이 없는 Stage 1 틀에는 과함 —
  실제 로직이 붙는 단계에서 필요하면 그때 Custom 위젯으로 교체).
  `ExamControlPanel`은 드롭다운/버튼 대신 라벨이 붙은 박스 4개(부위/방식/강도/실행)로,
  `CLIPanel`은 실제 `TMP_InputField` 대신 로그 텍스트 + 입력창처럼 보이는 박스로 자리만 잡아뒀다 —
  TMP_Dropdown/TMP_InputField는 내부 하이어라키가 깊고 이 프로젝트에 참고할 기존 인스턴스가 없어서
  수기 YAML로 재현하면 깨질 위험이 커 Stage 4/8 등 실제 로직이 붙는 시점으로 미뤘다.
- `Assets/Script/DI/BioSearchUIPresenter.cs`(신규) — `UIPresenter` 상속. `PostInitialize()`에서
  7개 패널을 `LoadPanel<T>(null, false, false)`(`IsStack:false`)로 순서대로 로드한 뒤
  `RentPanel<T>().OpenPanel()`로 연다 — `SceneUIManager.LoadPanel<T>()`의 단일 제네릭 오버로드는
  `OpenPanel()`을 자동으로 호출하지 않는다는 걸 소스 확인 후 반영. `UIPresenter.OpenPanelWithFade<T>()`는
  6장에 기록된 스택 최상단 버그 때문에 안 씀.
- `Assets/Script/DI/BioSearchCompositionRoot.cs` 수정 — `[SerializeField] BioSearchUIManager
  bioSearchUIManagerPrefab` 추가, `RegisterComponentInNewPrefab`으로 `SceneUIManager` 등록 +
  `RegisterEntryPoint<BioSearchUIPresenter>()` 추가.
- `Assets/Script/Room/ComputerViewController.cs` 수정 — `computerCanvasPrefab` 필드와 자체
  `Instantiate` 호출 제거. `[Inject] private void Construct(SceneUIManager sceneUiManager)`로
  `BioSearchCompositionRoot`가 만든 동일 인스턴스를 받아 `Canvas`/`GraphicRaycaster`만
  `GetComponent`로 꺼내 쓴다 — 캔버스를 두 번 인스턴스화하지 않도록.
- `ssh.unity` — `haare` GameObject(실제로는 `Processor` + `BioSearchCompositionRoot` 두 컴포넌트가
  붙어있는 오브젝트, 이름은 오해 소지 있음 — 다음에 이름 정리 고려)의 `BioSearchCompositionRoot`
  컴포넌트에 `bioSearchUIManagerPrefab` 참조 추가. `ComputerViewController`에서 삭제된
  `computerCanvasPrefab` 필드값 제거.
- `Assets/AddressableAssetsData/AssetGroups/Default Local Group.asset` — 7개 패널 프리팹을
  `Prefabs/UI/Panels/<PanelName>` 주소로 등록(`[PanelAttribute]` 경로와 일치, `AssetLoader.
  InstantiatePrefab`이 `Addressables.InstantiateAsync`를 쓰기 때문에 필수).

**검증 못한 부분**: 에디터를 직접 띄울 수 없어서 실제 Play 시 7개 패널이 뜨는지, Addressables
주소가 정상 인식되는지, YAML을 손으로 작성한 프리팹들이 깨진 참조 없이 열리는지는 확인하지
못했다 — 다음에 에디터에서 씬을 열고 콘솔 경고/에러 유무, Play 후 화면 레이아웃을 반드시 확인
필요.

**남은 일**: Stage 1 완료 기준상 로직은 없어도 되지만, 에디터 검증 후 이상 없으면 Stage 2(검사
사례 진행 구조: `CaseFile`/`CaseFileDefinition`/`CaseSessionService`)로 진행.
